import { TrendingUp } from 'lucide-react';

export const topicTimeseries = {
  id: "timeseries",
  name: "時系列分析",
  icon: TrendingUp,
  description: "時間順に並べられたデータの分析手法。AR・MA・ARIMAモデルやダービン・ワトソン検定を扱います。",
  sections: [
    {
      title: "時系列分析とは",
      content: `時系列（time series）とは、時間順に並べられた統計データのことです。例えば、毎月の販売額、毎日の気温、毎年のGDP などがあります。

**通常の横断面データとの違い**
通常のデータ分析では、観測値が互いに独立と仮定します。しかし時系列データは以下の特性があります：

1. 時間的依存性：隣同士の観測値は相関を持つ
2. トレンド：長期的な上昇や下降傾向
3. 季節性：定期的なパターン（例：冬は売上減など）
4. 非定常性：統計的性質が時間とともに変化

**自己相関の概念**
$t$ 時点のデータ $X_t$ と、$h$ 期先のデータ $X_{t+h}$ の相関を自己相関と言います。多くの経済・気象時系列では、近い時点ほど高い自己相関を持ちます。

**例：気温の時系列**
ある都市の毎月平均気温を12か月追跡すれば、隣接する月の気温は強く相関します（真冬から春へと連続的に変わる）。これは通常のランダムサンプルではあり得ない相関構造です。

**時系列分析の目的**
- 過去のパターン理解
- トレンド・季節性の分解
- 将来の予測
- 因果関係の分析（他系列との関連）`
    },
    {
      title: "弱定常過程と自己共分散",
      content: `統計学では、時間とともに性質が変わらない「定常過程」を扱いやすいため、多くの分析はこれを前提とします。

**弱定常性（wide-sense stationarity）**
プロセス $\\{X_t\\}$ が弱定常であるとは、以下を満たすことです：

1. 平均が一定：$E[X_t] = \\mu$ （すべての $t$）
2. 分散が一定：$\\text{Var}(X_t) = \\sigma^2$ （すべての $t$）
3. 自己共分散が時点に依存しない：$\\text{Cov}(X_t, X_{t+h}) = \\gamma_h$ （$h$ のみに依存）

つまり、時系列の統計的性質が時間に不変です。

**自己共分散関数 $\\gamma_h$**
$$\\gamma_h = \\text{Cov}(X_t, X_{t+h}) = E[(X_t - \\mu)(X_{t+h} - \\mu)]$$

$\\gamma_0 = \\text{Var}(X_t)$ は分散です。
$\\gamma_{-h} = \\gamma_h$ （対称性）

**自己相関関数 $\\rho_h$（ACF）**
自己共分散を標準化したものが自己相関関数：
$$\\rho_h = \\frac{\\gamma_h}{\\gamma_0} = \\frac{\\text{Cov}(X_t, X_{t+h})}{\\text{Var}(X_t)}$$

$-1 \\leq \\rho_h \\leq 1$
$\\rho_0 = 1$（常に）
$\\rho_{-h} = \\rho_h$

**例：ホワイトノイズ**
最も単純な定常過程はホワイトノイズ $\\varepsilon_t \\sim N(0, \\sigma^2)$ で、独立同分布です：
$$\\gamma_h = 0 \\quad (h \\neq 0)$$
$$\\rho_h = 0 \\quad (h \\neq 0)$$

これはランダムに見えるデータで、自己相関がありません。

**標本自己相関 $\\hat{r}_h$**
データ $x_1, x_2, \\ldots, x_n$ からは、標本平均 $\\bar{x}$ を使い：
$$\\hat{r}_h = \\frac{\\sum_{t=1}^{n-h} (x_t - \\bar{x})(x_{t+h} - \\bar{x})}{\\sum_{t=1}^{n} (x_t - \\bar{x})^2}$$

これは真の $\\rho_h$ を推定します。

**コレログラム**
ACFをプロットしたグラフがコレログラムです。このグラフでパターンを見ると、時系列の特性（AR/MA等）がわかります。`
    },
    {
      title: "AR(p)モデル",
      content: `AR($p$)（自己回帰）モデルは、現在値が過去の $p$ 期までの値に線形に依存するモデルです。

**AR(1)モデル**
最も単純な形は：
$$X_t = \\phi X_{t-1} + \\varepsilon_t$$

ここで $\\varepsilon_t \\sim N(0, \\sigma^2)$ はホワイトノイズ、$\\phi$ は自己回帰係数です。

これは過去1期の値に現在の値が依存することを意味します。

**定常条件**
AR(1)が定常であるには、$|\\phi| < 1$ が必要です。

$|\\phi| \\geq 1$ なら、過去のショックが永遠に影響を持ち、非定常になります。

例えば $\\phi = 1$（ランダムウォーク）なら $X_t = X_{t-1} + \\varepsilon_t$ で、分散が $t$ とともに増加します。

**自己相関関数 $\\rho_h$**
AR(1)の自己相関は指数関数的に減衰します：
$$\\rho_h = \\phi^h$$

例：$\\phi = 0.8$ なら、$\\rho_1 = 0.8$, $\\rho_2 = 0.64$, $\\rho_3 = 0.512$, ... と減少。

この減衰パターンはAR過程の特徴です。

**偏自己相関関数 PACF**
$\\text{PACF}(h)$ は、$X_{t-1}, \\ldots, X_{t-h+1}$ の効果を取り除いた後の、$X_t$ と $X_{t-h}$ の相関です。

AR($p$)モデルでは、PACFは lag $p$ までは0でなく、lag $p+1$ 以降は0になります。（シャープにカットオフ）
これでモデルの次数 $p$ を判定できます。

**AR($p$)の一般形**
$$X_t = \\phi_1 X_{t-1} + \\phi_2 X_{t-2} + \\cdots + \\phi_p X_{t-p} + \\varepsilon_t$$

特性方程式：
$$1 - \\phi_1 z - \\phi_2 z^2 - \\cdots - \\phi_p z^p = 0$$

のすべての根が単位円外（$|z| > 1$）ならば定常。

**推定**
AR係数 $\\phi_1, \\ldots, \\phi_p$ と分散 $\\sigma^2$ は、通常は Yule-Walker 方程式またはMLE（最尤法）で推定します。

Yule-Walker 方程式：
$$\\hat{\\rho}_h = \\hat{\\phi}_1 \\hat{\\rho}_{h-1} + \\hat{\\phi}_2 \\hat{\\rho}_{h-2} + \\cdots + \\hat{\\phi}_p \\hat{\\rho}_{h-p} \\quad (h = 1, 2, \\ldots, p)$$`
    },
    {
      title: "MA(q)モデル",
      content: `MA($q$)（移動平均）モデルは、現在値が過去の $q$ 期までのショック項に依存するモデルです。

**MA(1)モデル**
$$X_t = \\varepsilon_t + \\theta \\varepsilon_{t-1}$$

ここで $\\varepsilon_t \\sim N(0, \\sigma^2)$ はホワイトノイズ、$\\theta$ は移動平均係数です。

現在のショック $\\varepsilon_t$ と、1期前のショック $\\varepsilon_{t-1}$ が現在値に影響を与えます。

**定常性**
MA($q$)モデルは常に定常です。$\\theta$ の値にかかわらず、ショックの効果は有限の期間で消えます。

逆転可能性：
AR(1)との双対性のため、通常は $|\\theta| < 1$ を要求します（逆転可能性）。

**自己相関関数**
MA(1)のACFは：
$$\\rho_1 = \\frac{\\theta}{1 + \\theta^2}$$
$$\\rho_h = 0 \\quad (h \\geq 2)$$

重要：MAモデルはACFが $q$ で切れます。$h > q$ なら $\\rho_h = 0$。
これはAR過程の指数減衰と異なり、AR/MAを区別する手がかりになります。

**MA($q$)の一般形**
$$X_t = \\varepsilon_t + \\theta_1 \\varepsilon_{t-1} + \\theta_2 \\varepsilon_{t-2} + \\cdots + \\theta_q \\varepsilon_{t-q}$$

特性方程式：
$$1 + \\theta_1 z + \\theta_2 z^2 + \\cdots + \\theta_q z^q = 0$$

のすべての根が単位円外ならば逆転可能。

**ACFの特徴**
- $h > q$ で $\\rho_h = 0$（短い記憶）
- 直近のショックの影響が集中

例えば MA(2) なら、過去2期のショックのみが現在値に影響し、それ以前の影響はありません。

**推定**
MA係数は通常MLEで推定します。Yule-Walker法もありますが、MA過程では効率的ではありません。`
    },
    {
      title: "ARIMAモデルと差分",
      content: `非定常な時系列を扱う場合、まず差分を取って定常化した上で、ARMAを適用します。

**差分（differencing）**
1階差分（first difference）：
$$\\Delta X_t = X_t - X_{t-1}$$

これでトレンドを除去できます。

2階差分（second difference）：
$$\\Delta^2 X_t = \\Delta(\\Delta X_t) = (X_t - X_{t-1}) - (X_{t-1} - X_{t-2}) = X_t - 2X_{t-1} + X_{t-2}$$

季節差分（seasonal difference）：
$$\\nabla_{12} X_t = X_t - X_{t-12}$$

12か月周期なら、12期前の値を引いて季節性を除去。

**ARIMA($p, d, q$)モデル**
- $d$：差分の次数（Integrated の I）
- $p$：AR部分の次数
- $q$：MA部分の次数

手順：
1. 元の系列 $X_t$ を $d$ 回差分 → $Y_t = \\Delta^d X_t$
2. $Y_t$ に対し ARMA($p, q$) を当てはめ
3. パラメータ推定

**例：ARIMA(1,1,1)**
まず1階差分を取り $Y_t = \\Delta X_t$
その上で
$$Y_t = \\phi Y_{t-1} + \\varepsilon_t + \\theta \\varepsilon_{t-1}$$

元の $X_t$ に戻すには、推定値を累積和します。

**SARIMA($p, d, q$)($P, D, Q$)$_{12}$**
季節ARIMAモデルで、季節性を明示的に扱います。
例えば月次データで12か月周期がある場合、SARIMA(1,1,1)(1,1,1)$_{12}$ は：
- 月次AR(1) + 年次AR(1)
- 月次1階差分 + 年次1階差分
- 月次MA(1) + 年次MA(1)

**モデル識別**
ACFとPACFの形で $p, d, q$ を判定：
- ACFが徐々に減衰 → $d$ を増やして差分
- ACFが切れる（lag $q$ で0）→ MA($q$)
- PACFが切れる（lag $p$ で0）→ AR($p$)
- 両方が徐々に減衰 → ARMA`
    },
    {
      title: "ダービン・ワトソン検定",
      content: `回帰分析で得た残差に自己相関があるかを検定する方法がダービン・ワトソン（Durbin-Watson）検定です。

**背景**
OLS回帰では、残差 $\\varepsilon_t$ が互いに独立と仮定します。しかし時系列データでは、残差に自己相関がある場合があります。これを検出する必要があります。

**ダービン・ワトソン統計量**
残差 $e_t = y_t - \\hat{y}_t$ に対し、
$$DW = \\frac{\\sum_{t=2}^{n} (e_t - e_{t-1})^2}{\\sum_{t=1}^{n} e_t^2}$$

計算：
分子 $= \\sum$(連続する残差の差)$^2$
分母 $= \\sum$(残差)$^2$

**解釈**
$0 \\leq DW \\leq 4$

- $DW \\approx 2$：自己相関がない（理想的）
- $DW < 2$：正の自己相関（残差が連続して正または負）
- $DW > 2$：負の自己相関（残差が交互に正負）

**近似関係**
$$DW \\approx 2(1 - \\hat{\\rho}_1)$$

ここで $\\hat{\\rho}_1$ は1階自己相関係数：
$$\\hat{\\rho}_1 = \\frac{\\sum_{t=2}^{n} e_t e_{t-1}}{\\sum_{t=1}^{n} e_t^2}$$

$\\hat{\\rho}_1 = 0$ なら $DW \\approx 2$
$\\hat{\\rho}_1 = 1$ なら $DW \\approx 0$
$\\hat{\\rho}_1 = -1$ なら $DW \\approx 4$

**検定手順**
$H_0$：$\\rho_1 = 0$（自己相関なし）
$H_1$：$\\rho_1 \\neq 0$（自己相関あり）

臨界値 $d_L$（下側）と $d_U$（上側）は、$n$ とモデルの説明変数数 $k$ に依存する表から参照します。

- $DW < d_L$ ：正の自己相関あり（有意）
- $d_L \\leq DW \\leq d_U$ ：判断不可
- $d_U < DW < 4-d_U$ ：有意でない
- $4-d_U \\leq DW \\leq 4-d_L$ ：判断不可
- $DW > 4-d_L$ ：負の自己相関あり（有意）

【例題】$n = 50$, $k = 2$（説明変数1個）の回帰分析で $DW = 1.2$ が得られた。自己相関の有無を判定せよ。

【解答】
ダービン・ワトソン表から $d_L \\approx 1.46$, $d_U \\approx 1.63$

$DW = 1.2 < d_L = 1.46$ なので、正の自己相関が有意に存在します。

対策：AR項を追加するか、差分を取ってから回帰するなど。
【/解答】`
    },
    {
      title: "単位根検定 (Unit Root Tests)",
      content: `時系列分析において、データが定常か非定常かを判断することは極めて重要です。単位根検定はこの判断を統計的に行う手法です。

**単位根の概念**
AR(1)モデル $X_t = \\phi X_{t-1} + \\varepsilon_t$ において、$\\phi = 1$ のとき「単位根がある」と言います。

$\\phi = 1$ の場合、$X_t = X_{t-1} + \\varepsilon_t$（ランダムウォーク）となり：
- 分散が $\\text{Var}(X_t) = t\\sigma^2$ と時間とともに増大
- ショックの効果が永続する（減衰しない）
- 非定常過程となる

特性方程式 $1 - \\phi z = 0$ の根が $z = 1/\\phi = 1$（単位円上）であることから「単位根」と呼ばれます。

**ディッキー・フラー検定（Dickey-Fuller test; DF検定）**
AR(1)モデルを変形します：
$$\\Delta X_t = X_t - X_{t-1} = (\\phi-1)X_{t-1} + \\varepsilon_t = \\delta X_{t-1} + \\varepsilon_t$$

ここで $\\delta = \\phi - 1$ です。

帰無仮説と対立仮説：
$H_0$：$\\delta = 0$（$\\phi = 1$、単位根あり → 非定常）
$H_1$：$\\delta < 0$（$\\phi < 1$、単位根なし → 定常）

OLSで $\\delta$ を推定し、$t$ 統計量を計算します：
$$t_{DF} = \\frac{\\hat{\\delta}}{SE(\\hat{\\delta})}$$

**重要：検定統計量の帰無分布**
通常の回帰分析とは異なり、$H_0$（単位根あり）のもとでこの $t$ 統計量は標準的な $t$ 分布には従いません。ディッキー・フラー分布（DF分布）と呼ばれる特殊な分布に従います。

この分布は通常の $t$ 分布より左に偏っており（負の方向に裾が重い）、通常の $t$ 分布の臨界値を使うと単位根を棄却しすぎる誤りを犯します。

**DF統計量の臨界値（近似値）**

| 有意水準         | 1%    | 5%    | 10%   |
|-----------------|-------|-------|-------|
| 定数項なし       | -2.58 | -1.95 | -1.62 |
| 定数項あり       | -3.43 | -2.86 | -2.57 |
| 定数項+トレンド  | -3.96 | -3.41 | -3.13 |

（注：臨界値はサンプルサイズにも依存します。上記は大標本の近似値。）

**DF検定の3つのモデル**
1. 定数項もトレンドもなし：$\\Delta X_t = \\delta X_{t-1} + \\varepsilon_t$
2. 定数項あり：$\\Delta X_t = \\alpha + \\delta X_{t-1} + \\varepsilon_t$
3. 定数項+トレンドあり：$\\Delta X_t = \\alpha + \\beta t + \\delta X_{t-1} + \\varepsilon_t$

データの特性に応じて適切なモデルを選択します。

**拡張ディッキー・フラー検定（Augmented Dickey-Fuller test; ADF検定）**
基本のDF検定は、誤差項 $\\varepsilon_t$ が系列相関を持たない（ホワイトノイズ）ことを前提とします。現実のデータではこの仮定が満たされないことが多いため、ラグ付き差分項を追加して系列相関を吸収します：

$$\\Delta X_t = \\alpha + \\delta X_{t-1} + \\sum_{j=1}^{p} \\gamma_j \\Delta X_{t-j} + \\varepsilon_t$$

ラグ次数 $p$ の選択には AIC や BIC などの情報量基準を使います。検定統計量の帰無分布はDF検定と同じです。

**見せかけの回帰（Spurious Regression）**
非定常な時系列同士で回帰分析を行うと、本来無関係な変数間に見かけ上の有意な関係が検出されてしまう問題があります。

例えば、2つの独立なランダムウォーク $X_t$ と $Y_t$ に対して
$$Y_t = a + bX_t + u_t$$
を推定すると、$b$ が統計的に有意になる確率が非常に高くなります（サンプルサイズが大きいほど顕著）。

見せかけの回帰の兆候：
- $R^2$ が非常に高い
- DW統計量が非常に低い（残差に強い自己相関）
- $t$ 値が過大に有意

これを防ぐため、回帰分析の前に単位根検定を行い、非定常な変数は差分を取って定常化してから分析します。

**共和分（Cointegration）**
2つ以上の非定常な時系列が、ある線形結合によって定常となる場合、それらの系列は共和分の関係にあると言います。

例：$X_t$ と $Y_t$ がともに $I(1)$（1回差分で定常）であり、
$$Y_t - \\beta X_t = u_t \\quad (u_t \\text{ は定常})$$

となる $\\beta$ が存在するとき、$X_t$ と $Y_t$ は共和分している。

共和分の重要性：
- 見せかけの回帰ではなく、真の長期均衡関係を表す
- 誤差修正モデル（ECM）による短期・長期の動学分析が可能
- エングル・グレンジャー検定：回帰残差に対してADF検定を適用して共和分を検定

共和分は経済学で特に重要で、例えば消費と所得、為替レートと物価水準などの長期的関係の分析に用いられます。`
    },
    {
      title: "演習問題：ACFと時系列モデル選定",
      content: `【例題】以下は観測されたACFとPACFです。どのモデルが適切か答えよ。

ケース1：
ACF：$\\rho_1=0.7, \\rho_2=0.6, \\rho_3=0.5, \\rho_4=0.4$ → 指数的減衰
PACF：$\\phi_1=0.7, \\phi_2 \\approx 0, \\phi_3 \\approx 0, \\phi_4 \\approx 0$ → lag 1で切れる

ケース2：
ACF：$\\rho_1=0.5, \\rho_2=0.3, \\rho_3 \\approx 0, \\rho_4 \\approx 0$ → lag 2で切れる
PACF：$\\phi_1=0.5, \\phi_2=0.3, \\phi_3=0.2, \\phi_4=0.1$ → 指数的減衰

【解答】
ケース1：
- ACFが指数減衰 + PACFが lag 1で切れる → **AR(1)**

ケース2：
- ACFが lag 2で切れる + PACFが指数減衰 → **MA(2)**
【/解答】

---

【例題】AR(1)モデル $X_t = 0.6X_{t-1} + \\varepsilon_t$ を考える。
(1) $\\rho_1, \\rho_2, \\rho_3$ を計算せよ。
(2) 残差 $\\varepsilon_t \\sim N(0, \\sigma^2=4)$ のとき、$\\text{Var}(X_t)$ を求めよ。

【解答】
(1) $\\rho_h = \\phi^h$ より
$$\\rho_1 = 0.6^1 = 0.6$$
$$\\rho_2 = 0.6^2 = 0.36$$
$$\\rho_3 = 0.6^3 = 0.216$$

(2) AR(1)が定常なとき（$|\\phi|<1$），
$$\\text{Var}(X_t) = \\frac{\\sigma^2}{1 - \\phi^2} = \\frac{4}{1 - 0.36} = \\frac{4}{0.64} = 6.25$$
【/解答】

---

【例題】ある経済モデルの回帰で、残差が以下のように観測された。ダービン・ワトソン統計量を計算せよ。
$$e_t: 0.5, 0.8, 0.3, -0.2, -0.6, -0.4, 0.1, 0.2, -0.1, 0.4$$

【解答】
$$DW = \\frac{\\sum_{t=2}^{10} (e_t - e_{t-1})^2}{\\sum_{t=1}^{10} e_t^2}$$

分子の計算：
$$(0.8-0.5)^2 + (0.3-0.8)^2 + (-0.2-0.3)^2 + (-0.6+0.2)^2 + (-0.4+0.6)^2$$
$$+ (0.1+0.4)^2 + (0.2-0.1)^2 + (-0.1-0.2)^2 + (0.4+0.1)^2$$
$$= 0.09 + 0.25 + 0.25 + 0.16 + 0.04 + 0.25 + 0.01 + 0.09 + 0.25 = 1.39$$

分母の計算：
$$0.25 + 0.64 + 0.09 + 0.04 + 0.36 + 0.16 + 0.01 + 0.04 + 0.01 + 0.16 = 1.76$$

$$DW = \\frac{1.39}{1.76} \\approx 0.79$$

$DW \\approx 0.79 < 2$ なので、正の自己相関がある可能性。$n=10, k=1$（仮定）の場合、DW表から判定します。
【/解答】

---

【例題】AR(1)モデル $X_t = \\phi X_{t-1} + \\varepsilon_t$（$\\varepsilon_t \\sim \\text{WN}(0, \\sigma^2)$、$|\\phi| < 1$）について、自己共分散関数 $\\gamma_h = \\text{Cov}(X_t, X_{t+h})$ を導出せよ。

【解答】
**$\\gamma_0$ の導出：**
$X_t = \\phi X_{t-1} + \\varepsilon_t$ の両辺の分散を取ると：
$$\\text{Var}(X_t) = \\phi^2 \\text{Var}(X_{t-1}) + \\text{Var}(\\varepsilon_t)$$

定常性より $\\text{Var}(X_t) = \\text{Var}(X_{t-1}) = \\gamma_0$ なので：
$$\\gamma_0 = \\phi^2 \\gamma_0 + \\sigma^2$$
$$\\gamma_0 = \\frac{\\sigma^2}{1 - \\phi^2}$$

**$\\gamma_h$（$h \\geq 1$）の導出：**
$$\\gamma_h = \\text{Cov}(X_t, X_{t+h}) = \\text{Cov}(X_t, \\phi X_{t+h-1} + \\varepsilon_{t+h})$$
$$= \\phi \\, \\text{Cov}(X_t, X_{t+h-1}) + \\text{Cov}(X_t, \\varepsilon_{t+h})$$

$t < t+h$ より $X_t$ は $\\varepsilon_{t+h}$ と独立なので $\\text{Cov}(X_t, \\varepsilon_{t+h}) = 0$。よって：
$$\\gamma_h = \\phi \\gamma_{h-1}$$

これを再帰的に適用すると：
$$\\gamma_h = \\phi^h \\gamma_0 = \\frac{\\phi^h \\sigma^2}{1 - \\phi^2}$$

自己相関関数は：
$$\\rho_h = \\frac{\\gamma_h}{\\gamma_0} = \\phi^h$$

$|\\phi| < 1$ なので $\\rho_h$ は指数的に減衰します。
【/解答】

---

【例題】ARMA(1,1)モデル $X_t = \\phi X_{t-1} + \\varepsilon_t + \\theta \\varepsilon_{t-1}$ の定常性条件を示せ。

【解答】
ARMA($p, q$)モデルの定常性はAR部分のみで決まります。MA部分は定常性に影響しません（MA過程は常に定常）。

ARMA(1,1)のAR部分の特性方程式は：
$$1 - \\phi z = 0 \\implies z = \\frac{1}{\\phi}$$

定常性の条件は、この特性根が単位円の外にあること、すなわち：
$$|z| = \\left|\\frac{1}{\\phi}\\right| > 1 \\iff |\\phi| < 1$$

**定常性条件：$|\\phi| < 1$**

なお、逆転可能性（invertibility）はMA部分で決まり：
$$1 + \\theta z = 0 \\implies z = -\\frac{1}{\\theta}$$

逆転可能性の条件は $|\\theta| < 1$ です。

**ARMA(1,1)が定常かつ逆転可能であるための条件：**
$$|\\phi| < 1 \\quad \\text{かつ} \\quad |\\theta| < 1$$

定常性と逆転可能性は独立な概念であり、片方のみが成り立つ場合もあります。

定常であるとき、ARMA(1,1)の分散は：
$$\\gamma_0 = \\text{Var}(X_t) = \\frac{(1 + 2\\phi\\theta + \\theta^2)\\sigma^2}{1 - \\phi^2}$$
【/解答】

---

【例題】以下の時系列データについてADF検定を適用し、単位根の有無を判定せよ。

定数項ありモデル $\\Delta X_t = \\alpha + \\delta X_{t-1} + \\varepsilon_t$ をOLS推定した結果：
- $\\hat{\\delta} = -0.15$
- $SE(\\hat{\\delta}) = 0.08$
- 標本サイズ $n = 100$

【解答】
$t$ 統計量を計算：
$$t_{DF} = \\frac{\\hat{\\delta}}{SE(\\hat{\\delta})} = \\frac{-0.15}{0.08} = -1.875$$

定数項ありモデルのDF分布の臨界値（大標本近似）：
- 1%水準：$-3.43$
- 5%水準：$-2.86$
- 10%水準：$-2.57$

$t_{DF} = -1.875$ は10%水準の臨界値 $-2.57$ よりも大きい（絶対値が小さい）ので、いずれの有意水準でも帰無仮説（単位根あり）を棄却できません。

**結論：** この時系列には単位根がある（非定常である）と判断されます。

分析への示唆：
- この変数をそのまま回帰分析に用いると見せかけの回帰になる危険性がある
- 1階差分 $\\Delta X_t$ をとって再度ADF検定を行い、差分系列が定常か確認する
- 他の非定常変数との共和分関係の可能性を検討する
【/解答】

---

【例題】MA(1)モデル $X_t = \\varepsilon_t + \\theta \\varepsilon_{t-1}$（$\\varepsilon_t \\sim \\text{WN}(0, \\sigma^2)$）について、自己共分散関数 $\\gamma_0, \\gamma_1, \\gamma_h \\;(h \\geq 2)$ を導出せよ。

【解答】
**$\\gamma_0$ の導出：**
$$\\gamma_0 = \\text{Var}(X_t) = \\text{Var}(\\varepsilon_t + \\theta \\varepsilon_{t-1}) = \\sigma^2 + \\theta^2 \\sigma^2 = (1 + \\theta^2)\\sigma^2$$

**$\\gamma_1$ の導出：**
$$\\gamma_1 = \\text{Cov}(X_t, X_{t+1}) = \\text{Cov}(\\varepsilon_t + \\theta \\varepsilon_{t-1}, \\; \\varepsilon_{t+1} + \\theta \\varepsilon_t)$$

$\\varepsilon_t$ と $\\varepsilon_{t+1}$ は独立、$\\varepsilon_{t-1}$ と $\\varepsilon_{t+1}, \\varepsilon_t$ も独立であるから：
$$\\gamma_1 = \\text{Cov}(\\varepsilon_t, \\theta \\varepsilon_t) = \\theta \\sigma^2$$

**$\\gamma_h \\;(h \\geq 2)$ の導出：**
$h \\geq 2$ のとき、$X_t = \\varepsilon_t + \\theta \\varepsilon_{t-1}$ と $X_{t+h} = \\varepsilon_{t+h} + \\theta \\varepsilon_{t+h-1}$ は共通のホワイトノイズ項を含まないため：
$$\\gamma_h = 0 \\quad (h \\geq 2)$$

自己相関関数は：
$$\\rho_1 = \\frac{\\gamma_1}{\\gamma_0} = \\frac{\\theta}{1 + \\theta^2}, \\quad \\rho_h = 0 \\;(h \\geq 2)$$

これがMA(1)の特徴であり、ACFが lag 1 で切れることでMA(1)モデルを識別できる。
【/解答】

---

【例題】ARMA(1,1)モデル $X_t = \\phi X_{t-1} + \\varepsilon_t + \\theta \\varepsilon_{t-1}$（$|\\phi| < 1$, $\\varepsilon_t \\sim \\text{WN}(0, \\sigma^2)$）のスペクトル密度関数を求めよ。

【解答】
弱定常過程 $\\{X_t\\}$ のスペクトル密度関数は自己共分散関数のフーリエ変換として定義される：
$$f(\\omega) = \\frac{1}{2\\pi} \\sum_{h=-\\infty}^{\\infty} \\gamma_h e^{-ih\\omega}$$

ARMA(1,1)モデルは、後退シフト演算子 $B$ を用いて $(1 - \\phi B)X_t = (1 + \\theta B)\\varepsilon_t$ と表せる。

一般に ARMA($p,q$) のスペクトル密度は：
$$f(\\omega) = \\frac{\\sigma^2}{2\\pi} \\left| \\frac{\\Theta(e^{-i\\omega})}{\\Phi(e^{-i\\omega})} \\right|^2$$

ARMA(1,1) の場合、$\\Phi(z) = 1 - \\phi z$, $\\Theta(z) = 1 + \\theta z$ であるから：

$$f(\\omega) = \\frac{\\sigma^2}{2\\pi} \\cdot \\frac{|1 + \\theta e^{-i\\omega}|^2}{|1 - \\phi e^{-i\\omega}|^2}$$

分子を展開する：
$$|1 + \\theta e^{-i\\omega}|^2 = (1 + \\theta e^{-i\\omega})(1 + \\theta e^{i\\omega}) = 1 + 2\\theta \\cos\\omega + \\theta^2$$

分母を展開する：
$$|1 - \\phi e^{-i\\omega}|^2 = (1 - \\phi e^{-i\\omega})(1 - \\phi e^{i\\omega}) = 1 - 2\\phi \\cos\\omega + \\phi^2$$

したがって：
$$f(\\omega) = \\frac{\\sigma^2}{2\\pi} \\cdot \\frac{1 + 2\\theta \\cos\\omega + \\theta^2}{1 - 2\\phi \\cos\\omega + \\phi^2} \\quad (-\\pi \\leq \\omega \\leq \\pi)$$

$\\phi > 0$ のとき低周波数（$\\omega \\approx 0$）のパワーが大きく、$\\theta > 0$ のとき高周波数のパワーが相対的に増加する。
【/解答】

---

【例題】回帰分析で残差 $e_t$（$t=1,\\ldots,8$）が以下のように得られた。ダービン・ワトソン統計量を具体的に計算し、$\\hat{\\rho}_1$ との近似関係を確認せよ。
$$e_t: 1.2, \\; 0.8, \\; 0.3, \\; -0.4, \\; -0.9, \\; -0.5, \\; 0.2, \\; 0.7$$

【解答】
**分母の計算**（残差二乗和）：
$$\\sum_{t=1}^{8} e_t^2 = 1.44 + 0.64 + 0.09 + 0.16 + 0.81 + 0.25 + 0.04 + 0.49 = 3.92$$

**分子の計算**（連続差の二乗和）：
$$(0.8-1.2)^2 + (0.3-0.8)^2 + (-0.4-0.3)^2 + (-0.9+0.4)^2 + (-0.5+0.9)^2 + (0.2+0.5)^2 + (0.7-0.2)^2$$
$$= 0.16 + 0.25 + 0.49 + 0.25 + 0.16 + 0.49 + 0.25 = 2.05$$

$$DW = \\frac{2.05}{3.92} \\approx 0.523$$

**$\\hat{\\rho}_1$ の計算：**
$$\\hat{\\rho}_1 = \\frac{\\sum_{t=2}^{8} e_t e_{t-1}}{\\sum_{t=1}^{8} e_t^2} = \\frac{0.96 + 0.24 - 0.12 + 0.36 + 0.45 - 0.10 + 0.14}{3.92} = \\frac{1.93}{3.92} \\approx 0.492$$

**近似関係の確認：**
$$DW \\approx 2(1 - \\hat{\\rho}_1) = 2(1 - 0.492) = 2 \\times 0.508 = 1.016$$

実際の $DW = 0.523$ と近似値 $1.016$ にやや乖離がある。これは $n=8$ と標本サイズが小さいため近似が粗くなっているが、いずれにせよ $DW \\ll 2$ であり、強い正の自己相関が存在することを示している。
【/解答】`
    }
  ]
};
