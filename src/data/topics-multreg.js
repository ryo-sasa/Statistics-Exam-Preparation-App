import { LineChart } from 'lucide-react';

export const topicMultreg = {
  id: 'multreg',
  name: '重回帰分析',
  icon: LineChart,
  description: '複数の説明変数を用いた線形回帰',
  sections: [
    {
      title: '重回帰モデルと最小二乗推定',
      content: `**モデルの定義**
重回帰モデルは以下のように表現されます：

$$\\mathbf{Y} = \\mathbf{X}\\boldsymbol{\\beta} + \\boldsymbol{\\varepsilon}$$

ここで
- $\\mathbf{Y} = (Y_1, \\ldots, Y_n)^\\top$：目的変数ベクトル
- $\\mathbf{X}$：$n \\times (p+1)$ の計画行列。第1列は $1$（切片）、第 $j$ 列は説明変数 $X_j$
- $\\boldsymbol{\\beta} = (\\beta_0, \\beta_1, \\ldots, \\beta_p)^\\top$：回帰係数ベクトル
- $\\boldsymbol{\\varepsilon} \\sim N(\\mathbf{0}, \\sigma^2 \\mathbf{I}_n)$：誤差項

**最小二乗推定（OLS）**
残差二乗和 $S(\\boldsymbol{\\beta}) = \\|\\mathbf{Y} - \\mathbf{X}\\boldsymbol{\\beta}\\|^2$ を最小化する $\\hat{\\boldsymbol{\\beta}}$ は：

$$\\hat{\\boldsymbol{\\beta}} = (\\mathbf{X}^\\top\\mathbf{X})^{-1} \\mathbf{X}^\\top\\mathbf{Y}$$

これはフルランク条件 $\\text{rank}(\\mathbf{X}) = p+1$ の下で一意に定まります。

**ガウス-マルコフ定理**
ガウス-マルコフ定理により、OLS推定量 $\\hat{\\boldsymbol{\\beta}}$ は以下の性質を持ちます：
- 不偏性：$E[\\hat{\\boldsymbol{\\beta}}] = \\boldsymbol{\\beta}$
- 最良性：$\\text{Cov}(\\hat{\\boldsymbol{\\beta}}) = \\sigma^2 (\\mathbf{X}^\\top\\mathbf{X})^{-1}$ は全ての線形不偏推定量の中で最小

即ち、$\\hat{\\boldsymbol{\\beta}}$ はBLUE（最良線形不偏推定量）です。この性質は正規性を仮定せず成立します。

**推定値と残差**
予測値：$\\hat{\\mathbf{Y}} = \\mathbf{X}\\hat{\\boldsymbol{\\beta}} = \\mathbf{H}\\mathbf{Y}$（$\\mathbf{H} = \\mathbf{X}(\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top$ はハット行列）
残差：$\\mathbf{e} = \\mathbf{Y} - \\hat{\\mathbf{Y}} = (\\mathbf{I} - \\mathbf{H})\\mathbf{Y}$`
    },
    {
      title: '多重共線性と診断',
      content: `**多重共線性とは**
説明変数間に強い線形関係がある場合、多重共線性が存在します。その結果：
- $(\\mathbf{X}^\\top\\mathbf{X})^{-1}$ の要素が大きくなり、推定係数の標準誤差が増加
- 係数推定値が不安定になり、小さなデータ変化で大きく変動
- 検定の検出力が低下

**分散拡大係数（VIF）**
説明変数 $X_j$ のVIFは：

$$\\text{VIF}_j = \\frac{1}{1 - R_j^2}$$

ここで $R_j^2$ は $X_j$ を目的変数とし、他の説明変数を説明変数とする回帰の決定係数です。

$\\text{VIF} > 10$ で多重共線性の存在が疑われます。$\\text{VIF} > 5$ の場合も注意が必要です。

**条件数**
計画行列 $\\mathbf{X}$ の条件数は：

$$\\kappa = \\sqrt{\\frac{\\lambda_{\\max}}{\\lambda_{\\min}}}$$

ここで $\\lambda_{\\max}, \\lambda_{\\min}$ は $\\mathbf{X}^\\top\\mathbf{X}$ の最大・最小固有値です。$\\kappa > 30$ で多重共線性が問題になります。

**対処法**
- 相関の高い変数の1つを削除
- 主成分回帰：主成分を説明変数として使用
- リッジ回帰：$(\\mathbf{X}^\\top\\mathbf{X} + \\lambda\\mathbf{I})^{-1}\\mathbf{X}^\\top\\mathbf{Y}$ を利用（$\\lambda > 0$）
- LassoやElastic Netなど正則化手法`
    },
    {
      title: '変数選択',
      content: `**赤池情報量基準（AIC）**
説明変数の組み合わせに対するAICは：

$$\\text{AIC} = -2 \\log L + 2(p+1)$$

ここで $L$ は最尤推定値、$p+1$ はパラメータ数（切片を含む）です。AICが小さいモデルが好ましいとされます。

正規回帰の場合、以下と同等です：

$$\\text{AIC} = n \\log\\left(\\frac{\\text{SSR}}{n}\\right) + 2(p+1)$$

$\\text{SSR}$ は残差二乗和です。

**ベイズ情報量基準（BIC）**

$$\\text{BIC} = -2 \\log L + (p+1) \\log n$$

$$= n \\log\\left(\\frac{\\text{SSR}}{n}\\right) + (p+1) \\log n$$

BICはAICよりも複雑なモデルに対するペナルティが大きいため、より単純なモデルを選択する傾向があります。

**ステップワイズ法**
変数の追加・削除を繰り返して最適な変数集合を探索する方法です：

前進選択：初期モデルから変数を1つずつ追加。各ステップでAICを最小化する変数を追加し、追加によってAICが改善しなくなったら停止。

後退削除：全変数から開始し、1つずつ削除。各ステップでAICを最小化する削除を実行します。

双方向選択：前進選択と後退削除を組み合わせた方法です。`
    },
    {
      title: '残差分析と診断',
      content: `**線形性の確認**
散布図行列（scatterplot matrix）や残差 $e_i$ と予測値 $\\hat{Y}_i$ のプロット（残差プロット）を確認します。残差が $0$ を中心にランダムに分布していることが理想的です。パターンがあれば非線形性を示唆します。

**等分散性の確認**
残差プロット $e_i$ vs $\\hat{Y}_i$ で、残差の分散が予測値に依存しないかを確認します。分散が予測値に比例する場合、加重最小二乗法（WLS）の利用を検討します。

Breusch-Pagan検定：$e_i^2$ を説明変数に対して回帰し、F統計量により等分散性を検定します。

**正規性の確認**
Q-Qプロット：理論的な正規分布の分位点とサンプルの分位点をプロットします。点が直線上にあれば正規性が支持されます。

Shapiro-Wilk検定：正規性の仮説検定です。$p$ 値 $< 0.05$ で正規性を棄却します。

**独立性の確認**
Durbin-Watson統計量：

$$DW = \\frac{\\sum_{t=2}^{n}(e_t - e_{t-1})^2}{\\sum_{t=1}^{n} e_t^2}$$

$DW \\approx 2$ で残差は独立（自己相関なし）、$DW < 2$ で正の自己相関があります。時系列データでは必須の診断です。

**影響力のある観測値**
レバレッジ（leverage）：$h_{ii} = [\\mathbf{H}]_{ii}$。$h_{ii} > 2(p+1)/n$ で影響力が強い可能性。

Cookの距離：

$$D_i = \\frac{e_i^2}{(p+1)\\hat{\\sigma}^2} \\cdot \\frac{h_{ii}}{(1-h_{ii})^2}$$

$D_i > F_{0.5}(p+1, n-p-1)$ で当該観測値の影響が大きい。`
    },
    {
      title: '演習問題',
      content: `**問題1**：重回帰モデル $\\mathbf{Y} = \\mathbf{X}\\boldsymbol{\\beta} + \\boldsymbol{\\varepsilon}$ のOLS推定量 $\\hat{\\boldsymbol{\\beta}} = (\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top\\mathbf{Y}$ を導出し、ガウス-マルコフ定理の意味を説明してください。

**問題2**：$\\text{VIF} = 10$ の説明変数は何を示していますか？また、多重共線性が存在する場合の対処法を3つ述べてください。

**問題3**：AICとBICの違いを説明し、なぜBICの方がより単純なモデルを選択する傾向があるのか論述してください。

**問題4**：残差プロットで上向きの漏斗形が観察された場合、どのような問題が考えられ、どう対処すべきかを説明してください。

【例題】OLS推定量を行列表記で導出せよ
重回帰モデル $\\mathbf{Y} = \\mathbf{X}\\boldsymbol{\\beta} + \\boldsymbol{\\varepsilon}$ において、残差二乗和を最小化するOLS推定量を行列微分を用いて導出せよ。また、$\\hat{\\boldsymbol{\\beta}}$ の分散共分散行列を求めよ。

【解答】
残差二乗和は：

$$S(\\boldsymbol{\\beta}) = (\\mathbf{Y} - \\mathbf{X}\\boldsymbol{\\beta})^\\top(\\mathbf{Y} - \\mathbf{X}\\boldsymbol{\\beta}) = \\mathbf{Y}^\\top\\mathbf{Y} - 2\\boldsymbol{\\beta}^\\top\\mathbf{X}^\\top\\mathbf{Y} + \\boldsymbol{\\beta}^\\top\\mathbf{X}^\\top\\mathbf{X}\\boldsymbol{\\beta}$$

$\\boldsymbol{\\beta}$ で微分して $0$ とおく：

$$\\frac{\\partial S}{\\partial \\boldsymbol{\\beta}} = -2\\mathbf{X}^\\top\\mathbf{Y} + 2\\mathbf{X}^\\top\\mathbf{X}\\boldsymbol{\\beta} = \\mathbf{0}$$

正規方程式 $\\mathbf{X}^\\top\\mathbf{X}\\boldsymbol{\\beta} = \\mathbf{X}^\\top\\mathbf{Y}$ を解いて：

$$\\hat{\\boldsymbol{\\beta}} = (\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top\\mathbf{Y}$$

分散共分散行列は、$\\hat{\\boldsymbol{\\beta}} = (\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top\\mathbf{Y}$ に $\\mathbf{Y} = \\mathbf{X}\\boldsymbol{\\beta} + \\boldsymbol{\\varepsilon}$ を代入し：

$$\\text{Cov}(\\hat{\\boldsymbol{\\beta}}) = (\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top \\cdot \\sigma^2\\mathbf{I} \\cdot \\mathbf{X}(\\mathbf{X}^\\top\\mathbf{X})^{-1} = \\sigma^2(\\mathbf{X}^\\top\\mathbf{X})^{-1}$$
【/解答】

【例題】VIFを計算し多重共線性を診断せよ
3つの説明変数 $X_1, X_2, X_3$ を持つ重回帰モデルにおいて、$X_1$ を他の2変数で回帰したところ $R_1^2 = 0.92$、$X_2$ を他の2変数で回帰したところ $R_2^2 = 0.45$、$X_3$ を他の2変数で回帰したところ $R_3^2 = 0.88$ であった。各変数のVIFを計算し、多重共線性を診断せよ。

【解答】
各変数のVIFを計算する：

$$\\text{VIF}_1 = \\frac{1}{1 - R_1^2} = \\frac{1}{1 - 0.92} = \\frac{1}{0.08} = 12.5$$

$$\\text{VIF}_2 = \\frac{1}{1 - R_2^2} = \\frac{1}{1 - 0.45} = \\frac{1}{0.55} \\approx 1.82$$

$$\\text{VIF}_3 = \\frac{1}{1 - R_3^2} = \\frac{1}{1 - 0.88} = \\frac{1}{0.12} \\approx 8.33$$

診断結果：
- $X_1$：$\\text{VIF}_1 = 12.5 > 10$ であり、深刻な多重共線性が存在する。$X_1$ は他の変数とほぼ線形従属である。
- $X_2$：$\\text{VIF}_2 = 1.82 < 5$ であり、多重共線性の問題はない。
- $X_3$：$\\text{VIF}_3 = 8.33$ であり、$5 < \\text{VIF} < 10$ の範囲で注意が必要。

対処法として、$X_1$ と $X_3$ の間に強い相関があると考えられるため、いずれか一方を削除するか、リッジ回帰 $\\hat{\\boldsymbol{\\beta}}_{\\text{ridge}} = (\\mathbf{X}^\\top\\mathbf{X} + \\lambda\\mathbf{I})^{-1}\\mathbf{X}^\\top\\mathbf{Y}$ の適用を検討する。
【/解答】

【例題】AICとBICで変数選択を行え
$n = 50$ のデータに対し、以下の4つの回帰モデルの残差二乗和が得られた。AICとBICを計算し、最適なモデルを選択せよ。

| モデル | 説明変数の数 $p$ | $\\text{SSR}$ |
|---|---|---|
| A | 1 | 120 |
| B | 2 | 100 |
| C | 3 | 95 |
| D | 5 | 93 |

【解答】
$n = 50$ で、パラメータ数は $p+1$（切片含む）。$\\log 50 \\approx 3.912$ を用いる。

$$\\text{AIC} = n \\log\\left(\\frac{\\text{SSR}}{n}\\right) + 2(p+1)$$

$$\\text{BIC} = n \\log\\left(\\frac{\\text{SSR}}{n}\\right) + (p+1)\\log n$$

各モデルについて計算する：

**モデルA**（$p=1$）：
$\\text{AIC} = 50\\log(120/50) + 2 \\times 2 = 50 \\times 0.875 + 4 = 47.76$
$\\text{BIC} = 50 \\times 0.875 + 2 \\times 3.912 = 51.57$

**モデルB**（$p=2$）：
$\\text{AIC} = 50\\log(100/50) + 2 \\times 3 = 50 \\times 0.693 + 6 = 40.66$
$\\text{BIC} = 50 \\times 0.693 + 3 \\times 3.912 = 46.39$

**モデルC**（$p=3$）：
$\\text{AIC} = 50\\log(95/50) + 2 \\times 4 = 50 \\times 0.642 + 8 = 40.08$
$\\text{BIC} = 50 \\times 0.642 + 4 \\times 3.912 = 47.75$

**モデルD**（$p=5$）：
$\\text{AIC} = 50\\log(93/50) + 2 \\times 6 = 50 \\times 0.620 + 12 = 43.01$
$\\text{BIC} = 50 \\times 0.620 + 6 \\times 3.912 = 54.47$

| モデル | AIC | BIC |
|---|---|---|
| A | 47.76 | 51.57 |
| B | 40.66 | 46.39 |
| C | 40.08 | 47.75 |
| D | 43.01 | 54.47 |

AIC基準：モデルC（$\\text{AIC} = 40.08$）が最適。
BIC基準：モデルB（$\\text{BIC} = 46.39$）が最適。

BICはAICよりもペナルティ項が大きい（$\\log 50 \\approx 3.91 > 2$）ため、より単純なモデルBを選択した。実務的には、AICは予測精度の観点から、BICはモデルの真の次数を推定する観点から用いられる。
【/解答】

【例題】回帰係数のOLS推定を具体的な行列計算で求めよ。
以下の3個のデータに対し、単回帰モデル $Y_i = \\beta_0 + \\beta_1 X_i + \\varepsilon_i$ のOLS推定量を行列計算で求めよ。

| $i$ | $X_i$ | $Y_i$ |
|---|---|---|
| 1 | 1 | 3 |
| 2 | 2 | 5 |
| 3 | 3 | 8 |

【解答】
計画行列と目的変数ベクトルを構成する：

$$\\mathbf{X} = \\begin{pmatrix} 1 & 1 \\\\ 1 & 2 \\\\ 1 & 3 \\end{pmatrix}, \\quad \\mathbf{Y} = \\begin{pmatrix} 3 \\\\ 5 \\\\ 8 \\end{pmatrix}$$

$\\mathbf{X}^\\top\\mathbf{X}$ を計算する：

$$\\mathbf{X}^\\top\\mathbf{X} = \\begin{pmatrix} 1 & 1 & 1 \\\\ 1 & 2 & 3 \\end{pmatrix}\\begin{pmatrix} 1 & 1 \\\\ 1 & 2 \\\\ 1 & 3 \\end{pmatrix} = \\begin{pmatrix} 3 & 6 \\\\ 6 & 14 \\end{pmatrix}$$

$(\\mathbf{X}^\\top\\mathbf{X})^{-1}$ を計算する。$|\\mathbf{X}^\\top\\mathbf{X}| = 42 - 36 = 6$ より：

$$(\\mathbf{X}^\\top\\mathbf{X})^{-1} = \\frac{1}{6}\\begin{pmatrix} 14 & -6 \\\\ -6 & 3 \\end{pmatrix} = \\begin{pmatrix} 7/3 & -1 \\\\ -1 & 1/2 \\end{pmatrix}$$

$\\mathbf{X}^\\top\\mathbf{Y}$ を計算する：

$$\\mathbf{X}^\\top\\mathbf{Y} = \\begin{pmatrix} 1 & 1 & 1 \\\\ 1 & 2 & 3 \\end{pmatrix}\\begin{pmatrix} 3 \\\\ 5 \\\\ 8 \\end{pmatrix} = \\begin{pmatrix} 16 \\\\ 37 \\end{pmatrix}$$

OLS推定量：

$$\\hat{\\boldsymbol{\\beta}} = (\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top\\mathbf{Y} = \\begin{pmatrix} 7/3 & -1 \\\\ -1 & 1/2 \\end{pmatrix}\\begin{pmatrix} 16 \\\\ 37 \\end{pmatrix} = \\begin{pmatrix} 112/3 - 37 \\\\ -16 + 37/2 \\end{pmatrix} = \\begin{pmatrix} -1/3 \\\\ 5/2 \\end{pmatrix}$$

したがって $\\hat{\\beta}_0 = -1/3 \\approx -0.333$, $\\hat{\\beta}_1 = 5/2 = 2.5$ であり、回帰式は $\\hat{Y} = -0.333 + 2.5X$ となる。
【/解答】

【例題】決定係数と自由度調整済み決定係数を計算し比較せよ。
$n = 20$ のデータに対して、説明変数が $p = 2$ の重回帰モデルを当てはめた結果、全変動 $\\text{SST} = 500$、残差変動 $\\text{SSR} = 120$ であった。決定係数 $R^2$ と自由度調整済み決定係数 $R_{\\text{adj}}^2$ を計算し、両者の差を解釈せよ。

【解答】
決定係数：

$$R^2 = 1 - \\frac{\\text{SSR}}{\\text{SST}} = 1 - \\frac{120}{500} = 1 - 0.24 = 0.76$$

自由度調整済み決定係数：

$$R_{\\text{adj}}^2 = 1 - \\frac{\\text{SSR}/(n - p - 1)}{\\text{SST}/(n - 1)} = 1 - \\frac{120/17}{500/19} = 1 - \\frac{7.059}{26.316} = 1 - 0.2682 = 0.7318$$

$R^2 = 0.76$ に対して $R_{\\text{adj}}^2 = 0.732$ であり、差は $0.028$ である。

$R_{\\text{adj}}^2$ は $R^2$ より小さくなる。これは変数を追加すると $R^2$ は必ず増加するが、$R_{\\text{adj}}^2$ は自由度の損失を補正するため、不要な変数の追加では減少しうるからである。$R_{\\text{adj}}^2$ は：

$$R_{\\text{adj}}^2 = 1 - (1 - R^2)\\frac{n-1}{n-p-1} = 1 - 0.24 \\times \\frac{19}{17} = 1 - 0.2682 = 0.732$$

と等価である。標本サイズ $n$ が小さいほど、また説明変数 $p$ が大きいほど、$R^2$ と $R_{\\text{adj}}^2$ の差は大きくなる。
【/解答】

【例題】ダービン・ワトソン統計量を計算し自己相関を判定せよ。
重回帰モデルの残差が時系列順に以下のとおり得られた。

$$e_1 = 2.1,\\; e_2 = 1.5,\\; e_3 = 0.8,\\; e_4 = -0.3,\\; e_5 = -1.2,\\; e_6 = -0.9,\\; e_7 = 0.4,\\; e_8 = 1.3$$

ダービン・ワトソン統計量 $DW$ を計算し、正の自己相関の有無を判定せよ（$n = 8$, $p = 2$, 有意水準5%の下限値 $d_L = 0.763$, 上限値 $d_U = 1.332$）。

【解答】
ダービン・ワトソン統計量の定義：

$$DW = \\frac{\\sum_{t=2}^{n}(e_t - e_{t-1})^2}{\\sum_{t=1}^{n}e_t^2}$$

分子を計算する：

$$(1.5-2.1)^2 + (0.8-1.5)^2 + (-0.3-0.8)^2 + (-1.2+0.3)^2 + (-0.9+1.2)^2 + (0.4+0.9)^2 + (1.3-0.4)^2$$

$$= 0.36 + 0.49 + 1.21 + 0.81 + 0.09 + 1.69 + 0.81 = 5.46$$

分母を計算する：

$$2.1^2 + 1.5^2 + 0.8^2 + 0.3^2 + 1.2^2 + 0.9^2 + 0.4^2 + 1.3^2$$

$$= 4.41 + 2.25 + 0.64 + 0.09 + 1.44 + 0.81 + 0.16 + 1.69 = 11.49$$

$$DW = \\frac{5.46}{11.49} \\approx 0.475$$

判定：$DW = 0.475 < d_L = 0.763$ であるから、正の自己相関が存在すると結論する。残差が時系列方向に正の系列相関を示しており、誤差の独立性仮定が満たされていない。一般化最小二乗法（GLS）やコクラン・オーカット法による補正が必要である。
【/解答】

【例題】残差プロットから回帰モデルの仮定違反を診断せよ。
重回帰分析の結果、以下の残差と予測値が得られた。

| $i$ | $\\hat{Y}_i$ | $e_i$ | $e_i^2$ |
|---|---|---|---|
| 1 | 10 | $-0.5$ | 0.25 |
| 2 | 15 | $-1.2$ | 1.44 |
| 3 | 20 | 2.5 | 6.25 |
| 4 | 25 | $-3.8$ | 14.44 |
| 5 | 30 | 4.1 | 16.81 |
| 6 | 35 | $-5.0$ | 25.00 |

残差の絶対値が $\\hat{Y}_i$ の増加とともに大きくなっている。Breusch-Pagan検定の考え方を用いて、等分散性の検定を行え。

【解答】
残差プロットにおいて、残差の絶対値が予測値とともに増加しており、分散の不均一性（heteroscedasticity）が疑われる。

Breusch-Pagan検定の手順：$e_i^2$ を $\\hat{Y}_i$ に回帰する。

$\\hat{Y}$ の平均：$\\bar{\\hat{Y}} = \\frac{10+15+20+25+30+35}{6} = 22.5$

$e_i^2$ の平均：$\\bar{e^2} = \\frac{0.25 + 1.44 + 6.25 + 14.44 + 16.81 + 25.00}{6} = \\frac{64.19}{6} = 10.698$

回帰係数：

$$\\hat{\\beta}_1 = \\frac{\\sum(\\hat{Y}_i - \\bar{\\hat{Y}})(e_i^2 - \\bar{e^2})}{\\sum(\\hat{Y}_i - \\bar{\\hat{Y}})^2}$$

分子：$(-12.5)(-10.45) + (-7.5)(-9.26) + (-2.5)(-4.45) + (2.5)(3.74) + (7.5)(6.11) + (12.5)(14.30)$
$= 130.6 + 69.4 + 11.1 + 9.4 + 45.8 + 178.8 = 445.1$

分母：$156.25 + 56.25 + 6.25 + 6.25 + 56.25 + 156.25 = 437.5$

$$\\hat{\\beta}_1 = \\frac{445.1}{437.5} \\approx 1.017$$

$e_i^2$ の $\\hat{Y}_i$ に対する回帰の決定係数 $R_{BP}^2$ を計算すると高い値が得られる。検定統計量は $\\text{BP} = nR_{BP}^2$ で $\\chi^2(1)$ に従う。

この結果は分散が予測値に依存して増加していることを示す。対処法として：
- 対数変換 $\\log Y$ への変換
- 加重最小二乗法（WLS）：$w_i = 1/\\hat{Y}_i^2$ 等の重み
- 分散安定化変換（Box-Cox変換）

のいずれかを適用すべきである。
【/解答】

---

【例題】重回帰モデル $Y = \\beta_0 + \\beta_1 X_1 + \\beta_2 X_2 + \\varepsilon$ において、偏回帰係数 $\\beta_1$ の意味を単回帰係数との違いを含めて説明せよ。以下のデータを用いて具体的に計算せよ。

| $i$ | $X_1$ | $X_2$ | $Y$ |
|---|---|---|---|
| 1 | 2 | 10 | 25 |
| 2 | 4 | 20 | 45 |
| 3 | 6 | 15 | 40 |
| 4 | 8 | 25 | 60 |
| 5 | 10 | 30 | 75 |

【解答】
**偏回帰係数の意味：**
偏回帰係数 $\\beta_1$ は、$X_2$ を一定に保ったときの $X_1$ の1単位増加に対する $Y$ の変化量である。一方、単回帰係数 $b_1$（$Y$ を $X_1$ のみで回帰）は $X_2$ の影響を制御しておらず、$X_1$ と $X_2$ が相関している場合、$X_2$ の間接的な効果も含んでしまう。

**単回帰の計算：**
$\\bar{X}_1 = 6$, $\\bar{Y} = 49$

$$b_1 = \\frac{\\sum(X_{1i} - \\bar{X}_1)(Y_i - \\bar{Y})}{\\sum(X_{1i} - \\bar{X}_1)^2} = \\frac{(-4)(-24)+(-2)(-4)+(0)(-9)+(2)(11)+(4)(26)}{16+4+0+4+16}$$

$$= \\frac{96 + 8 + 0 + 22 + 104}{40} = \\frac{230}{40} = 5.75$$

**偏回帰係数の意味の違い：**
単回帰では $b_1 = 5.75$ であるが、重回帰で $X_2$ を追加すると、$X_1$ と $X_2$ の相関の影響が除かれ、$\\hat{\\beta}_1$ は一般に $b_1$ とは異なる値をとる。$X_1$ と $X_2$ に正の相関があれば、単回帰係数は $X_2$ の効果を一部含むため過大推定される傾向がある。

偏回帰係数は「他の変数を統制した上での純粋な効果」を表しており、因果推論において重要な概念である。
【/解答】

---

【例題】以下の重回帰モデルの結果から、各観測値のてこ比（leverage）$h_{ii}$ を計算し、影響力の強い観測値を特定せよ。

$n = 5$, $p = 1$（説明変数1個）、計画行列：
$$\\mathbf{X} = \\begin{pmatrix} 1 & 1 \\\\ 1 & 2 \\\\ 1 & 3 \\\\ 1 & 4 \\\\ 1 & 10 \\end{pmatrix}$$

【解答】
ハット行列 $\\mathbf{H} = \\mathbf{X}(\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top$ の対角要素がてこ比 $h_{ii}$ である。

$$\\mathbf{X}^\\top\\mathbf{X} = \\begin{pmatrix} 5 & 20 \\\\ 20 & 130 \\end{pmatrix}$$

$|\\mathbf{X}^\\top\\mathbf{X}| = 650 - 400 = 250$

$$(\\mathbf{X}^\\top\\mathbf{X})^{-1} = \\frac{1}{250}\\begin{pmatrix} 130 & -20 \\\\ -20 & 5 \\end{pmatrix} = \\begin{pmatrix} 0.52 & -0.08 \\\\ -0.08 & 0.02 \\end{pmatrix}$$

てこ比は $h_{ii} = \\mathbf{x}_i^\\top (\\mathbf{X}^\\top\\mathbf{X})^{-1} \\mathbf{x}_i$ で計算する。$\\bar{X}_1 = 4$ を用いた公式：

$$h_{ii} = \\frac{1}{n} + \\frac{(X_{1i} - \\bar{X}_1)^2}{\\sum_j (X_{1j} - \\bar{X}_1)^2}$$

$\\sum(X_{1j} - 4)^2 = 9 + 4 + 1 + 0 + 36 = 50$

各観測のてこ比：
- $h_{11} = 1/5 + 9/50 = 0.20 + 0.18 = 0.38$
- $h_{22} = 1/5 + 4/50 = 0.20 + 0.08 = 0.28$
- $h_{33} = 1/5 + 1/50 = 0.20 + 0.02 = 0.22$
- $h_{44} = 1/5 + 0/50 = 0.20 + 0.00 = 0.20$
- $h_{55} = 1/5 + 36/50 = 0.20 + 0.72 = 0.92$

判定基準：$h_{ii} > 2(p+1)/n = 2 \\times 2/5 = 0.8$

$h_{55} = 0.92 > 0.8$ であり、第5観測値（$X_1 = 10$）は高いてこ比を持つ影響力のある観測値である。この点は他のデータから大きく外れた $X$ 値を持ち、回帰直線を引き寄せる力が強い。
【/解答】

---

【例題】以下の重回帰分析の結果から、クックの距離 $D_i$ を計算し、回帰に対して過度に影響力のある観測値を検出せよ。

$n = 6$, $p = 2$（説明変数2個）、$\\hat{\\sigma}^2 = 4.0$

| $i$ | 残差 $e_i$ | てこ比 $h_{ii}$ |
|---|---|---|
| 1 | 0.5 | 0.25 |
| 2 | $-1.2$ | 0.30 |
| 3 | 3.8 | 0.55 |
| 4 | $-0.3$ | 0.20 |
| 5 | 0.8 | 0.22 |
| 6 | $-0.6$ | 0.28 |

【解答】
クックの距離の公式：
$$D_i = \\frac{e_i^2}{(p+1)\\hat{\\sigma}^2} \\cdot \\frac{h_{ii}}{(1 - h_{ii})^2}$$

$p+1 = 3$, $\\hat{\\sigma}^2 = 4.0$ であるから $(p+1)\\hat{\\sigma}^2 = 12.0$

各観測値について計算する：

$D_1 = \\frac{0.25}{12} \\times \\frac{0.25}{(0.75)^2} = 0.0208 \\times 0.444 = 0.009$

$D_2 = \\frac{1.44}{12} \\times \\frac{0.30}{(0.70)^2} = 0.120 \\times 0.612 = 0.073$

$D_3 = \\frac{14.44}{12} \\times \\frac{0.55}{(0.45)^2} = 1.203 \\times 2.716 = 3.268$

$D_4 = \\frac{0.09}{12} \\times \\frac{0.20}{(0.80)^2} = 0.0075 \\times 0.3125 = 0.002$

$D_5 = \\frac{0.64}{12} \\times \\frac{0.22}{(0.78)^2} = 0.0533 \\times 0.362 = 0.019$

$D_6 = \\frac{0.36}{12} \\times \\frac{0.28}{(0.72)^2} = 0.030 \\times 0.540 = 0.016$

判定基準として $D_i > 4/n = 4/6 \\approx 0.667$ または $D_i > F_{0.5}(p+1, n-p-1) = F_{0.5}(3, 3) \\approx 0.765$ を用いる。

$D_3 = 3.268$ は基準値を大幅に超えており、第3観測値は回帰結果に極めて大きな影響を与えている。これは残差が大きく（$e_3 = 3.8$）かつてこ比も高い（$h_{33} = 0.55$）ことに起因する。この観測値を除外して再分析し、結果が安定するか確認すべきである。
【/解答】`
    }
  ]
};
