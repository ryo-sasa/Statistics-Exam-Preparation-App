import { Binary } from 'lucide-react';
export const topicQualreg = {
  id: 'qualreg',
  name: '質的回帰',
  icon: Binary,
  description: '2値・多値・計数応答変数を扱う回帰モデルと一般化線形モデル',
  sections: [
    {
      title: 'ロジスティック回帰',
      content: `**概要**
ロジスティック回帰は、応答変数が2値（0または1、成功/失敗、はい/いいえ）である場合の回帰モデルです。線形回帰では予測値が $[0,1]$ 外になる可能性があるため、確率を0〜1に制限するロジスティック関数を用います。

**ロジスティック関数**
確率 $p = P(Y=1|X)$ は以下のロジスティック関数で表されます：

$$p = \\frac{1}{1 + e^{-\\eta}} = \\frac{e^{\\eta}}{1 + e^{\\eta}}$$

ここで $\\eta = \\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p$ は線形予測子です。

この関数の特性：
- $\\eta \\to \\infty$ のとき、$p \\to 1$
- $\\eta \\to -\\infty$ のとき、$p \\to 0$
- $\\eta = 0$ のとき、$p = 0.5$

**ロジット変換**
確率 $p$ のロジット（ロジスティック変換）は：

$$\\text{logit}(p) = \\log\\frac{p}{1-p} = \\eta = \\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p$$

この変換により、確率 $[0,1]$ を実数直線 $(-\\infty, \\infty)$ に線形化します。

**オッズとオッズ比**
オッズは成功の確率と失敗の確率の比：

$$\\text{Odds} = \\frac{p}{1-p} = e^{\\eta} = \\exp(\\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p)$$

説明変数 $X_j$ が1単位増加するとき、オッズの変化比（オッズ比）は：

$$\\text{OR} = e^{\\beta_j} = \\frac{\\text{Odds}(X_j=x+1)}{\\text{Odds}(X_j=x)}$$

例えば、$e^{\\beta_1} = 1.5$ なら、$X_1$ が1単位増加するとオッズが50%増加します。

**最尤推定（Maximum Likelihood Estimation）**
パラメータ $\\beta$ は以下の尤度関数を最大化して推定します：

$$L(\\beta) = \\prod_{i} p_i^{y_i}(1-p_i)^{1-y_i}$$

対数尤度：

$$\\ell(\\beta) = \\sum_{i} \\left[ y_i \\log(p_i) + (1-y_i) \\log(1-p_i) \\right]$$

ここで $p_i = \\frac{1}{1 + \\exp(-(\\beta_0 + \\sum_j \\beta_j X_{ij}))}$ です。

**反復加重最小二乗法（IRLS）**
最尤推定は通常、以下の加重最小二乗問題を反復的に解くことで求められます：

$$\\hat{\\beta}^{(t+1)} = (X^\\top W^{(t)} X)^{-1} X^\\top W^{(t)} z^{(t)}$$

ここで $W$ は重み行列（対角要素は $p_i(1-p_i)$）、$z$ は調整応答変数です。

【例題】過去のデータで、年収 $X$ とクレジットカード申込承認 $Y$（1=承認、0=却下）の関係が $\\text{logit}(p) = -10 + 0.05X$（$X$ は万円単位）で与えられている。年収300万円の場合の承認確率を求めよ。

【解答】
$$\\text{logit}(p) = -10 + 0.05 \\times 300 = 5$$

$$p = \\frac{e^5}{1+e^5} \\approx 0.993$$

年収300万円の場合、承認確率は約99.3%です。
【/解答】

【例題】ロジスティック回帰：$\\text{logit}(\\text{罹患}) = -2 + 0.03 \\times \\text{年齢} + 0.5 \\times \\text{喫煙フラグ}$。50歳で非喫煙者の場合、罹患確率を計算してください。

【解答】
$$\\text{logit} = -2 + 0.03 \\times 50 + 0.5 \\times 0 = -0.5$$

$$p = \\frac{1}{1 + e^{0.5}} = \\frac{1}{1 + 1.649} \\approx 0.378$$

罹患確率は約37.8%です。
【/解答】`
    },
    {
      title: 'プロビット回帰',
      content: `**概要**
プロビット回帰は、ロジスティック回帰の代替モデルで、標準正規分布の累積分布関数を使用します。応答変数が0または1の場合に適用されます。

**プロビット関数**
確率 $p = P(Y=1|X)$ は標準正規分布の累積分布関数 $\\Phi$ で表されます：

$$p = \\Phi(\\eta) = \\Phi(\\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p)$$

つまり：

$$p = \\int_{-\\infty}^{\\eta} \\frac{1}{\\sqrt{2\\pi}} e^{-t^2/2} \\, dt$$

逆関数はprobit変換：

$$\\Phi^{-1}(p) = \\eta = \\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p$$

**潜在変数の解釈**
プロビットモデルは以下の潜在変数モデルとして解釈できます：

$$Y^* = \\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p + \\varepsilon, \\quad \\varepsilon \\sim N(0,1)$$

$Y = 1$ if $Y^* > 0$, $Y = 0$ otherwise

ここで $Y^*$ は観測不可能な潜在変数で、例えば商品購買の「購買意欲」など。

**ロジスティック回帰との比較**
- ロジスティック分布：より裾が重い（外れ値の影響に強い）
- 正規分布（プロビット）：中心に集中（外れ値の影響に弱い）
実務では、両者の予測精度はしばしば似ていますが、裾の振る舞いが異なります。

両モデルのパラメータは直接比較不可（スケールが異なる）ですが、限界効果（Marginal Effect）は近い値になることが多いです。

**最尤推定**
ロジスティック回帰と同様、尤度関数を最大化してパラメータを推定します。ただし、$\\Phi$ は解析的な逆関数を持たないため、数値計算が必要です。

【例題】プロビットモデル：$\\Phi^{-1}(p_{\\text{就業}}) = -1 + 0.1 \\times \\text{教育年数}$。12年の教育（高卒）の場合の就業確率を求めよ。

【解答】
$$\\Phi^{-1}(p) = -1 + 0.1 \\times 12 = 0.2$$

$$p = \\Phi(0.2) \\approx 0.579$$

就業確率は約57.9%です。
【/解答】

【例題】プロビット回帰：$\\Phi^{-1}(\\text{購買}) = -0.5 + 0.02 \\times \\text{価格（千円）}$。商品価格が30,000円の場合、購買確率は？

【解答】
$$\\Phi^{-1}(p) = -0.5 + 0.02 \\times 30 = 0.1$$

$$p = \\Phi(0.1) \\approx 0.540$$

購買確率は約54.0%です。
【/解答】`
    },
    {
      title: 'ポアソン回帰',
      content: `**概要**
ポアソン回帰は、応答変数が非負整数の計数データ（0, 1, 2, ...）である場合に用いられます。例えば、一定期間の事故件数、顧客来店数、疾病発症数などをモデル化します。

**モデルの設定**
応答変数 $Y \\sim \\text{Poisson}(\\lambda)$ で、期待値 $E[Y|X] = \\lambda$ は説明変数 $X$ に依存します。線形リンク関数では $\\lambda$ が負になる可能性があるため、対数リンク関数を使用：

$$\\log(\\lambda) = \\eta = \\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p$$

したがって：

$$\\lambda = \\exp(\\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p)$$

説明変数 $X_j$ が1単位増加するとき、期待カウント数の変化率：

$$e^{\\beta_j} = \\frac{E[Y|X_1,\\ldots,X_j+1,\\ldots]}{E[Y|X_1,\\ldots,X_j,\\ldots]}$$

例えば、$e^{\\beta_1} = 1.1$ なら、$X_1$ が1増加すると期待カウントが10%増加。

**分散と過分散**
ポアソン分布の特性：$E[Y] = \\text{Var}(Y) = \\lambda$（平均と分散が等しい）

実データでは分散が平均より大きいことが多く、これを過分散（Overdispersion）と呼びます。

過分散が見られる場合の対策：
- 負の二項分布モデルの使用：$\\text{Var}(Y) = \\lambda + \\phi\\lambda^2$（$\\phi > 0$）
- GLMにおける分散パラメータの推定：$\\text{Var}(Y) = \\phi \\times \\lambda$

**最尤推定**
パラメータ $\\beta$ は以下の尤度関数で推定：

$$L(\\beta) = \\prod_{i} \\frac{\\lambda_i^{y_i} e^{-\\lambda_i}}{y_i!}$$

対数尤度：

$$\\ell(\\beta) = \\sum_{i} \\left[ y_i(\\beta_0 + \\sum_j \\beta_j X_{ij}) - \\exp(\\beta_0 + \\sum_j \\beta_j X_{ij}) - \\log(y_i!) \\right]$$

この最大化も反復加重最小二乗法で解きます。

【例題】ポアソン回帰：$\\log(\\lambda_{\\text{事故}}) = 2 + 0.01 \\times \\text{交通量（千台）}$。月間交通量が50,000台の場合の月平均事故件数を求めよ。

【解答】
$$\\log(\\lambda) = 2 + 0.01 \\times 50 = 2.5$$

$$\\lambda = e^{2.5} \\approx 12.2$$

月平均約12.2件の事故が予測されます。
【/解答】

【例題】ポアソン回帰：$\\log(\\lambda_{\\text{問合}}) = 1 + 0.05 \\times \\text{従業員数}$。従業員50名の企業での問い合わせ件数の期待値は？

【解答】
$$\\log(\\lambda) = 1 + 0.05 \\times 50 = 3.5$$

$$\\lambda = e^{3.5} \\approx 33.1$$

月平均33件程度の問い合わせが予測されます。
【/解答】`
    },
    {
      title: '一般化線形モデル（GLM）',
      content: `**概要**
一般化線形モデル（Generalized Linear Model, GLM）は、ロジスティック、プロビット、ポアソン回帰を統一的に扱う枠組みです。正規分布に限定されない様々な応答分布に対応できます。

**GLMの3つの要素**
1. **確率分布**：応答変数 $Y$ の分布（正規、二項、ポアソン、ガンマなど）
2. **線形予測子**：$\\eta = \\beta_0 + \\beta_1 X_1 + \\cdots + \\beta_p X_p$
3. **リンク関数**：期待値 $\\mu = E[Y|X]$ と線形予測子を結びつける関数 $g(\\mu) = \\eta$

典型的なリンク関数：
- 正規分布：恒等関数 $g(\\mu) = \\mu$（通常の線形回帰）
- 二項分布：ロジット $g(\\mu) = \\log(\\mu/(1-\\mu))$ または プロビット $g(\\mu) = \\Phi^{-1}(\\mu)$
- ポアソン分布：対数関数 $g(\\mu) = \\log(\\mu)$
- ガンマ分布：逆数 $g(\\mu) = 1/\\mu$

**分散関数と尤度**
各分布には分散関数 $V(\\mu)$ があり、$\\text{Var}(Y) = V(\\mu)$ です。
例：正規分布 $V(\\mu) = \\sigma^2$（定数）、ポアソン $V(\\mu) = \\mu$、二項 $V(\\mu) = \\mu(1-\\mu)$

最尤推定は反復加重最小二乗法で解きます：

$$\\hat{\\beta}^{(t+1)} = (X^\\top W^{(t)} X)^{-1} X^\\top W^{(t)} z^{(t)}$$

ここで $W$ の対角要素は $d\\mu/d\\eta \\div V(\\mu)$、$z$ は調整応答です。

**モデルの評価**
逸脱度（Deviance）：

$$D = -2(\\ell(\\hat{\\beta}) - \\ell(\\text{飽和モデル}))$$

飽和モデル（各データに1パラメータ）との対数尤度差。$\\chi^2$ 分布で近似して有意性を検定。

AIC（赤池情報量基準）：

$$\\text{AIC} = 2p - 2\\ell(\\hat{\\beta})$$

モデル比較に用いられます。

**GLMの利点**
統一的な枠組みで、様々な応答型のデータに対応でき、推定方法や検定方法が一貫している。

【例題】3つのモデル候補があります。AIC値が(1) 245、(2) 238、(3) 242。最も優れたモデルはどれですか？

【解答】
AICが最も小さいモデル(2)（AIC = 238）が最適です。AICはモデルの適合度と複雑度のバランスを評価し、値が小さいほど良いモデルとされます。
【/解答】`
    },
    {
      title: '演習問題',
      content: `**問題1：ロジスティック回帰とオッズ比**
ある消費者調査で、テレビ広告の視聴有無（$X=1$:視聴、$0$:非視聴）と商品購買（$Y=1$:購買、$0$:非購買）の関係を分析。ロジスティック回帰の結果、$\\hat{\\beta}_0 = -1$, $\\hat{\\beta}_1 = 1.5$ が得られました。

(1) 広告を視聴した場合の購買確率を計算してください。
(2) オッズ比を求め、広告視聴による購買確率の変化を説明してください。
(3) 広告を非視聴の場合の購買確率は？

**問題2：ポアソン回帰の解釈**
ポアソン回帰で、$\\log(\\lambda) = 0.5 + 0.02X_1 - 0.01X_2$ の結果が得られました。
$X_1$ が商品価格（千円）、$X_2$ が顧客年齢です。

(1) $X_1$（価格）が1千円増加した場合、期待カウント数の変化率は？
(2) $X_2$（年齢）が10歳増加した場合、期待カウント数の変化率は？
(3) 価格10千円、年齢40歳の場合の期待カウント数は？

**問題3：リンク関数の選択**
以下のデータにはどのGLMが適切ですか？理由と共に答えてください。
(1) 過去1年間の入院日数（0日から365日の整数値）
(2) テスト成績が合格基準を満たすか否か（0または1）
(3) 毎月の交通事故件数（0以上の整数）

**問題4：過分散の検出**
ポアソン回帰を適用したモデルで、残差逸脱度が150、自由度が80の場合、
(1) 逸脱度の平均（Pearson's $\\chi^2$）を計算してください。
(2) 過分散が存在しますか？

**問題5：プロビット vs ロジスティック回帰**
相互扶助の参加有無（$Y$）を教育年数（$X$）で予測する2つのモデルを比較：
- ロジスティック：$\\text{logit}(p) = -2 + 0.2X$
- プロビット：$\\Phi^{-1}(p) = -1 + 0.1X$

$X=10$（高卒）のとき、各モデルの予測確率を計算し、結果を比較してください。

**問題6：ロジスティック回帰のオッズ比を求め解釈せよ**
【例題】ある疾病の発症（$Y=1$）に対するロジスティック回帰分析の結果、以下の推定値が得られた。
- $\\hat{\\beta}_0 = -3.0$（切片）
- $\\hat{\\beta}_1 = 0.8$（喫煙：$X_1=1$ 喫煙者、$0$ 非喫煙者）
- $\\hat{\\beta}_2 = 0.04$（年齢：$X_2$、連続変数）

(1) 喫煙のオッズ比を求め、解釈せよ。
(2) 50歳の喫煙者の発症確率を求めよ。

【解答】
(1) 喫煙のオッズ比：

$$\\text{OR} = e^{\\hat{\\beta}_1} = e^{0.8} \\approx 2.23$$

解釈：年齢を調整した上で、喫煙者は非喫煙者と比較して発症のオッズが約2.23倍高い。すなわち、喫煙は発症リスクを有意に高める因子である。

(2) 50歳の喫煙者（$X_1=1, X_2=50$）：

$$\\eta = -3.0 + 0.8 \\times 1 + 0.04 \\times 50 = -3.0 + 0.8 + 2.0 = -0.2$$

$$p = \\frac{1}{1+e^{0.2}} = \\frac{1}{1+1.221} \\approx 0.450$$

50歳の喫煙者の発症確率は約45.0%である。
【/解答】

**問題7：ポアソン回帰モデルの回帰係数を解釈せよ**
【例題】ある都市の月別交通事故件数 $Y$ を、降水日数 $X_1$ と平均気温 $X_2$（℃）で回帰したポアソンモデルの結果が以下の通りである：

$$\\log(\\hat{\\lambda}) = 2.5 + 0.06 X_1 - 0.03 X_2$$

(1) 降水日数が1日増えた場合の事故件数の変化率を求めよ。
(2) 降水日数15日、平均気温20℃の月の予測事故件数を求めよ。

【解答】
(1) 降水日数の回帰係数 $\\hat{\\beta}_1 = 0.06$ について：

$$e^{0.06} \\approx 1.062$$

降水日数が1日増加すると、事故件数の期待値は約6.2%増加する。

(2) $X_1 = 15$, $X_2 = 20$ のとき：

$$\\log(\\hat{\\lambda}) = 2.5 + 0.06 \\times 15 - 0.03 \\times 20 = 2.5 + 0.9 - 0.6 = 2.8$$

$$\\hat{\\lambda} = e^{2.8} \\approx 16.4$$

予測事故件数は月平均約16.4件である。
【/解答】

**問題8：デビアンスとAICによるモデル比較**
【例題】ある計数データに対して3つのポアソン回帰モデルを当てはめた。以下の結果からモデルを比較・選択せよ。

| モデル | パラメータ数 $p$ | 残差逸脱度 $D$ | 自由度 | AIC |
|--------|-------------|------------|--------|-----|
| M1（$X_1$ のみ） | 2 | 85.3 | 48 | 210.5 |
| M2（$X_1, X_2$） | 3 | 52.1 | 47 | 180.3 |
| M3（$X_1, X_2, X_3$） | 4 | 50.8 | 46 | 181.0 |

(1) M1からM2への逸脱度の変化量を求め、$X_2$ の追加が有意か $\\chi^2$ 検定を用いて判定せよ（有意水準5%）。
(2) AICに基づき最適なモデルを選択せよ。

【解答】
(1) 逸脱度の変化量：

$$\\Delta D = D_{M1} - D_{M2} = 85.3 - 52.1 = 33.2$$

自由度の変化：$\\Delta df = 48 - 47 = 1$

$\\chi^2_{0.05}(1) = 3.841$ であるから、$33.2 > 3.841$ より $X_2$ の追加は有意水準5%で有意である。$X_2$ はモデルの改善に寄与する。

(2) AICの比較：M2のAIC = 180.3 が最小であるため、AIC基準ではM2が最適である。M3は $X_3$ を追加しているが、AICは181.0とM2より大きく、パラメータ増加の効果が適合度の改善を上回っていないことを示す。
【/解答】`
    }
  ]
};
