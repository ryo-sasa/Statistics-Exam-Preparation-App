import { Binary } from 'lucide-react';
export const topicQualreg = {
  id: 'qualreg',
  name: '質的回帰',
  icon: Binary,
  description: '2値・多値・計数応答変数を扱う回帰モデルと一般化線形モデル',
  sections: [
    {
      title: 'ロジスティック回帰',
      content: `**概要**
ロジスティック回帰は、応答変数が2値（0または1、成功/失敗、はい/いいえ）である場合の回帰モデルです。線形回帰では予測値が[0,1]外になる可能性があるため、確率を0～1に制限するロジスティック関数を用います。

**ロジスティック関数**
確率 p = P(Y=1|X) は以下のロジスティック関数で表されます：
  p = 1 / (1 + exp(-η))、または p = exp(η) / (1 + exp(η))
ここで η = β₀ + β₁X₁ + ... + βₚXₚ は線形予測子です。

この関数の特性：
- η → ∞ のとき、p → 1
- η → -∞ のとき、p → 0
- η = 0 のとき、p = 0.5

**ロジット変換**
確率 p のロジット（ロジスティック変換）は：
  logit(p) = log(p/(1-p)) = η = β₀ + β₁X₁ + ... + βₚXₚ
この変換により、確率 [0,1] を実数直線 (-∞,∞) に線形化します。

**オッズとオッズ比**
オッズは成功の確率と失敗の確率の比：
  Odds = p/(1-p) = exp(η) = exp(β₀ + β₁X₁ + ... + βₚXₚ)
説明変数Xⱼが1単位増加するとき、オッズの変化比（オッズ比）は：
  exp(βⱼ) = Odds(X=x+1) / Odds(X=x)
例えば、exp(β₁) = 1.5 なら、X₁が1単位増加するとオッズが50%増加します。

**最尤推定（Maximum Likelihood Estimation）**
パラメータβは以下の尤度関数を最大化して推定します：
  L(β) = ∏ᵢ pᵢʸⁱ(1-pᵢ)^(1-yᵢ)
対数尤度：
  ℓ(β) = Σᵢ [yᵢ log(pᵢ) + (1-yᵢ) log(1-pᵢ)]
ここで pᵢ = 1/(1 + exp(-(β₀ + Σⱼ βⱼXᵢⱼ))) です。

**反復加重最小二乗法（IRLS）**
最尤推定は通常、以下の加重最小二乗問題を反復的に解くことで求められます：
  β^(t+1) = (X'W^(t)X)⁻¹ X'W^(t)z^(t)
ここで W は重み行列（対角要素は pᵢ(1-pᵢ)）、z は調整応答変数です。

**例：顧客のクレジットカード申込**
過去のデータで、年収Xと申込承認Y（1=承認、0=却下）の関係：
logit(p) = -10 + 0.05X（Xは万円単位）
年収300万円の場合：logit(p) = -10 + 0.05×300 = 5 → p = exp(5)/(1+exp(5)) ≈ 0.99（99%承認）

**例：病気の診断予測**
問題：ロジスティック回帰：logit(罹患) = -2 + 0.03×年齢 + 0.5×喫煙フラグ。50歳で非喫煙者の場合、罹患確率を計算してください。
解答：logit = -2 + 0.03×50 + 0.5×0 = -0.5 → p = 1/(1 + exp(0.5)) = 1/(1 + 1.649) ≈ 0.378（37.8%）`
    },
    {
      title: 'プロビット回帰',
      content: `**概要**
プロビット回帰は、ロジスティック回帰の代替モデルで、標準正規分布の累積分布関数を使用します。応答変数が0または1の場合に適用されます。

**プロビット関数**
確率 p = P(Y=1|X) は標準正規分布の累積分布関数Φで表されます：
  p = Φ(η) = Φ(β₀ + β₁X₁ + ... + βₚXₚ)
つまり、p = ∫₋∞^η (1/√(2π)) × exp(-t²/2) dt

逆関数はprobit変換：
  Φ⁻¹(p) = η = β₀ + β₁X₁ + ... + βₚXₚ

**潜在変数の解釈**
プロビットモデルは以下の潜在変数モデルとして解釈できます：
  Y* = β₀ + β₁X₁ + ... + βₚXₚ + ε、ε ~ N(0,1)
  Y = 1 if Y* > 0, Y = 0 otherwise
ここで Y* は観測不可能な潜在変数で、例えば商品購買の「購買意欲」など。

**ロジスティック回帰との比較**
- ロジスティック分布：より裾が重い（外れ値の影響に強い）
- 正規分布（プロビット）：中心に集中（外れ値の影響に弱い）
実務では、両者の予測精度はしばしば似ていますが、裾の振る舞いが異なります。

両モデルのパラメータは直接比較不可（スケールが異なる）ですが、限界効果（Marginal Effect）は近い値になることが多いです。

**最尤推定**
ロジスティック回帰と同様、尤度関数を最大化してパラメータを推定します。ただし、Φ は解析的な逆関数を持たないため、数値計算が必要です。

**例：教育と就業選択**
プロビットモデル：Φ⁻¹(p就業) = -1 + 0.1×教育年数
12年の教育（高卒）：Φ⁻¹(p) = -1 + 0.1×12 = 0.2 → p = Φ(0.2) ≈ 0.579（58%就業）

**例：商品購買の決定**
問題：プロビット回帰：Φ⁻¹(購買) = -0.5 + 0.02×価格_千円。商品価格が30,000円の場合、購買確率は？
解答：Φ⁻¹(p) = -0.5 + 0.02×30 = 0.1 → p = Φ(0.1) ≈ 0.540（54%の購買確率）`
    },
    {
      title: 'ポアソン回帰',
      content: `**概要**
ポアソン回帰は、応答変数が非負整数の計数データ（0, 1, 2, ...）である場合に用いられます。例えば、一定期間の事故件数、顧客来店数、疾病発症数などをモデル化します。

**モデルの設定**
応答変数 Y ~ Poisson(λ) で、期待値 E[Y|X] = λ は説明変数Xに依存します。線形リンク関数では λ が負になる可能性があるため、対数リンク関数を使用：
  log(λ) = η = β₀ + β₁X₁ + ... + βₚXₚ
したがって：
  λ = exp(β₀ + β₁X₁ + ... + βₚXₚ)

説明変数Xⱼが1単位増加するとき、期待カウント数の変化率：
  exp(βⱼ) = E[Y|X₁,...,Xⱼ+1,...] / E[Y|X₁,...,Xⱼ,...]
例えば、exp(β₁) = 1.1 なら、X₁が1増加すると期待カウントが10%増加。

**分散と過分散**
ポアソン分布の特性：E[Y] = V[Y] = λ（平均と分散が等しい）
実データでは分散が平均より大きいことが多く、これを過分散（Overdispersion）と呼びます。

過分散が見られる場合の対策：
- 負の二項分布モデルの使用：V[Y] = λ + φλ²（φ > 0）
- GLMにおける分散パラメータの推定：V[Y] = φ × λ

**最尤推定**
パラメータβは以下の尤度関数で推定：
  L(β) = ∏ᵢ (λᵢ^yᵢ × exp(-λᵢ)) / yᵢ!
対数尤度：
  ℓ(β) = Σᵢ [yᵢ(β₀ + Σⱼ βⱼXᵢⱼ) - exp(β₀ + Σⱼ βⱼXᵢⱼ) - log(yᵢ!)]
この最大化も反復加重最小二乗法で解きます。

**例：交通事故発生件数**
ポアソン回帰：log(λ事故) = 2 + 0.01×交通量_千台
月間交通量が50,000台の場合：log(λ) = 2 + 0.01×50 = 2.5 → λ = e^2.5 ≈ 12.2（月平均12.2件の事故）

**例：カスタマーサービスへの問い合わせ**
問題：ポアソン回帰：log(λ問合) = 1 + 0.05×従業員数。従業員50名の企業での問い合わせ件数の期待値は？
解答：log(λ) = 1 + 0.05×50 = 3.5 → λ = e^3.5 ≈ 33.1（月平均33件程度の問い合わせ）`
    },
    {
      title: '一般化線形モデル（GLM）',
      content: `**概要**
一般化線形モデル（Generalized Linear Model, GLM）は、ロジスティック、プロビット、ポアソン回帰を統一的に扱う枠組みです。正規分布に限定されない様々な応答分布に対応できます。

**GLMの3つの要素**
1. **確率分布**：応答変数Yの分布（正規、二項、ポアソン、ガンマなど）
2. **線形予測子**：η = β₀ + β₁X₁ + ... + βₚXₚ
3. **リンク関数**：期待値 μ = E[Y|X] と線形予測子を結びつける関数
   g(μ) = η

典型的なリンク関数：
- 正規分布：恒等関数 g(μ) = μ（通常の線形回帰）
- 二項分布：ロジット g(μ) = log(μ/(1-μ)) または プロビット g(μ) = Φ⁻¹(μ)
- ポアソン分布：対数関数 g(μ) = log(μ)
- ガンマ分布：逆数 g(μ) = 1/μ

**分散関数と尤度**
各分布には分散関数 V(μ) があり、V[Y] = V(μ) です。
例：正規分布 V(μ) = σ²（定数）、ポアソン V(μ) = μ、二項 V(μ) = μ(1-μ)

最尤推定は反復加重最小二乗法で解きます：
  β^(t+1) = (X'W^(t)X)⁻¹ X'W^(t)z^(t)
ここで W の対角要素は dμ/dη / V(μ)、z は調整応答です。

**モデルの評価**
逸脱度（Deviance）：
  D = -2(ℓ(β̂) - ℓ(飽和モデル))
飽和モデル（各データに1パラメータ）との対数尤度差。χ²分布で近似して有意性を検定。

AIC（赤池情報量基準）：
  AIC = 2p - 2ℓ(β̂)
モデル比較に用いられます。

**GLMの利点**
統一的な枠組みで、様々な応答型のデータに対応でき、推定方法や検定方法が一貫している。

**例：GLMの比較選択**
問題：3つのモデル候補があります。AIC値が(1) 245、(2) 238、(3) 242。最も優れたモデルはどれですか？
解答：AICが最も小さいモデル(2)が最適。AICはモデルの適合度と複雑度のバランスを評価します。`
    },
    {
      title: '演習問題',
      content: `**問題1：ロジスティック回帰とオッズ比**
ある消費者調査で、テレビ広告の視聴有無（X=1:視聴、0:非視聴）と商品購買（Y=1:購買、0:非購買）の関係を分析。ロジスティック回帰の結果、β₀ = -1, β₁ = 1.5 が得られました。

(1) 広告を視聴した場合の購買確率を計算してください。
(2) オッズ比を求め、広告視聴による購買確率の変化を説明してください。
(3) 広告を非視聴の場合の購買確率は？

**問題2：ポアソン回帰の解釈**
ポアソン回帰で、log(λ) = 0.5 + 0.02X₁ - 0.01X₂ の結果が得られました。
X₁が商品価格（千円）、X₂が顧客年齢です。

(1) X₁（価格）が1千円増加した場合、期待カウント数の変化率は？
(2) X₂（年齢）が10歳増加した場合、期待カウント数の変化率は？
(3) 価格10千円、年齢40歳の場合の期待カウント数は？

**問題3：リンク関数の選択**
以下のデータにはどのGLMが適切ですか？理由と共に答えてください。
(1) 過去1年間の入院日数（0日から365日の整数値）
(2) テスト成績が合格基準を満たすか否か（0または1）
(3) 毎月の交通事故件数（0以上の整数）

**問題4：過分散の検出**
ポアソン回帰を適用したモデルで、残差逸脱度が150、自由度が80の場合、
(1) 逸脱度の平均（Pearson's χ²）を計算してください。
(2) 過分散が存在しますか？**問題5：プロビット vs ロジスティック回帰**
相互扶助の参加有無（Y）を教育年数（X）で予測する2つのモデルを比較：
- ロジスティック：logit(p) = -2 + 0.2X
- プロビット：Φ⁻¹(p) = -1 + 0.1X

X=10（高卒）のとき、各モデルの予測確率を計算し、結果を比較してください。`
    }
  ]
};
