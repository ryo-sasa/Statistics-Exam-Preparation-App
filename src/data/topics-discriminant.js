import { Split } from 'lucide-react';

export const topicDiscriminant = {
  id: 'discriminant',
  name: '判別分析',
  icon: Split,
  description: 'グループ分類のための統計手法',
  sections: [
    {
      title: '線形判別分析（Fisher の LDA）',
      content: `**問題設定**
$p$ 次元の説明変数 $\\mathbf{X} = (X_1, \\dots, X_p)^\\top$ から、$k$ 個のグループのいずれかに個体を分類します。グループ $g$ の平均ベクトルを $\\boldsymbol{\\mu}_g$、共通の共分散行列を $\\boldsymbol{\\Sigma}$ とします。

**Fisher の判別関数**
Fisher の線形判別関数は、グループ間の分散とグループ内の分散の比を最大化する線形結合 $\\mathbf{a}^\\top \\mathbf{X}$ を探索します：

$$\\max_{\\mathbf{a}} \\frac{\\mathbf{a}^\\top \\boldsymbol{\\Sigma}_B \\mathbf{a}}{\\mathbf{a}^\\top \\boldsymbol{\\Sigma}_W \\mathbf{a}}$$

ここで $\\boldsymbol{\\Sigma}_B$ はグループ間分散行列、$\\boldsymbol{\\Sigma}_W$ はグループ内分散行列です。

**判別係数の推定**
2グループの場合、最適な判別係数 $\\mathbf{a}$ は以下で得られます：

$$\\mathbf{a} = \\boldsymbol{\\Sigma}_W^{-1} (\\boldsymbol{\\mu}_1 - \\boldsymbol{\\mu}_2)$$

多グループの場合は一般固有値問題 $\\boldsymbol{\\Sigma}_B \\mathbf{a} = \\lambda \\boldsymbol{\\Sigma}_W \\mathbf{a}$ を解きます。

**判別スコアと分類ルール**
個体 $\\mathbf{x}$ のスコアを $Z = \\mathbf{a}^\\top \\mathbf{x}$ として計算し、グループ1とグループ2の境界値 $Z_0$ と比較して分類します：

$$\\text{グループ1に分類} \\iff Z > Z_0$$

$$\\text{グループ2に分類} \\iff Z \\leq Z_0$$

ここで $Z_0 = \\frac{1}{2}\\mathbf{a}^\\top(\\boldsymbol{\\mu}_1 + \\boldsymbol{\\mu}_2)$ です。

**多グループの拡張**
$k > 2$ グループの場合、複数の判別関数を計算します（最大 $k-1$ 個）。各グループに対する判別スコア $Z_g = \\mathbf{c}_g^\\top \\mathbf{x}$ を計算し、最大値を与えるグループに分類します。`
    },
    {
      title: 'マハラノビス距離による判別',
      content: `**マハラノビス距離に基づく判別**
グループ $g$ からのマハラノビス距離 $D_g^2$ を計算し、最も距離が小さいグループに分類する方法です：

$$D_g^2 = (\\mathbf{x} - \\boldsymbol{\\mu}_g)^\\top \\boldsymbol{\\Sigma}^{-1} (\\mathbf{x} - \\boldsymbol{\\mu}_g)$$

個体 $\\mathbf{x}$ は $\\arg\\min_g D_g^2$ のグループに分類されます。

**確率的解釈**
グループ $g$ の事前確率を $\\pi_g$、密度関数を $f_g$ とするとき、ベイズの定理により：

$$P(\\text{グループ } g \\mid \\mathbf{x}) = \\frac{\\pi_g f_g(\\mathbf{x})}{\\sum_k \\pi_k f_k(\\mathbf{x})}$$

正規分布を仮定すれば、この事後確率はマハラノビス距離に関連付けられます。

**判別スコア関数**
線形判別スコア関数は：

$$\\delta_g(\\mathbf{x}) = \\boldsymbol{\\mu}_g^\\top \\boldsymbol{\\Sigma}^{-1} \\mathbf{x} - \\frac{1}{2} \\boldsymbol{\\mu}_g^\\top \\boldsymbol{\\Sigma}^{-1} \\boldsymbol{\\mu}_g + \\log \\pi_g$$

この関数が最大のグループに分類します。

**図形的意味**
マハラノビス距離は楕円球面の等距離曲線を描きます。共分散構造を反映した距離測定により、より適切な分類が実現されます。

**事前確率の影響**
事前確率 $\\pi_g$ は判別関数に $\\log \\pi_g$ として反映されるため、不均衡データの場合、調整が必要です。`
    },
    {
      title: '誤判別率の推定',
      content: `**再代入誤判別率（Apparent Error Rate）**
訓練データ自体に対する誤判別率：

$$\\mathrm{AER} = \\frac{\\text{判別関数で誤分類される訓練データの数}}{\\text{全訓練データ数}}$$

この推定量は、判別関数が訓練データに過適合する傾向があるため、テストデータに対する実際の誤判別率を過小評価します。

**ホールドアウト法**
データを訓練セット（通常70-80%）とテストセット（20-30%）に分割し、訓練セットで判別関数を構築し、テストセットで評価します：

$$\\mathrm{TEE} = \\frac{\\text{テストセットでの誤分類数}}{\\text{テストセット数}}$$

この推定量はより不偏ですが、サンプルサイズが限定される場合、分散が大きくなります。

**Leave-One-Out 交差検証（LOOCV）**
$n$ 個のデータに対して、以下を繰り返します：
1. $i$ 番目のデータを除いた $n-1$ 個のデータで判別関数を構築
2. $i$ 番目のデータで予測し、誤分類かどうかを記録
3. すべての $i = 1, \\dots, n$ に対して繰り返す

$$\\mathrm{CV} = \\frac{1}{n} \\sum_{i=1}^{n} I(\\hat{y}_{(i)} \\neq y_i)$$

ここで $\\hat{y}_{(i)}$ は $i$ 番目を除いて構築した判別関数による予測、$I(\\cdot)$ は指示関数です。これは計算量が多いですが、不偏で安定した推定量です。

**$k$ 分割交差検証**
LOOCV の計算負荷を軽減するため、データを $k$ 個の分割に分け、各分割をテストセットとする方法です（通常 $k=5$ または $k=10$）。`
    },
    {
      title: '例題',
      content: `【例題】フィッシャーの判別関数を求め判別境界を決定せよ。

2次元データ $(X_1, X_2)$ で2群の判別を行う。各群の標本平均と共通のプール共分散行列が以下のとおり与えられている：

$$\\bar{\\mathbf{x}}_1 = \\begin{pmatrix} 2 \\\\ 3 \\end{pmatrix}, \\quad \\bar{\\mathbf{x}}_2 = \\begin{pmatrix} 6 \\\\ 5 \\end{pmatrix}, \\quad \\boldsymbol{S}_W = \\begin{pmatrix} 4 & 1 \\\\ 1 & 2 \\end{pmatrix}$$

Fisher の判別係数を求め、新しい個体 $\\mathbf{x}_0 = (4, 5)^\\top$ をどちらの群に判別すべきか決定せよ。事前確率は等しい（$\\pi_1 = \\pi_2 = 0.5$）とする。

【解答】
Fisher の判別係数は $\\mathbf{a} = \\boldsymbol{S}_W^{-1}(\\bar{\\mathbf{x}}_1 - \\bar{\\mathbf{x}}_2)$ で与えられる。

まず逆行列を求める：

$$\\boldsymbol{S}_W^{-1} = \\frac{1}{4 \\times 2 - 1^2}\\begin{pmatrix} 2 & -1 \\\\ -1 & 4 \\end{pmatrix} = \\frac{1}{7}\\begin{pmatrix} 2 & -1 \\\\ -1 & 4 \\end{pmatrix}$$

$$\\bar{\\mathbf{x}}_1 - \\bar{\\mathbf{x}}_2 = \\begin{pmatrix} 2 - 6 \\\\ 3 - 5 \\end{pmatrix} = \\begin{pmatrix} -4 \\\\ -2 \\end{pmatrix}$$

$$\\mathbf{a} = \\frac{1}{7}\\begin{pmatrix} 2 & -1 \\\\ -1 & 4 \\end{pmatrix}\\begin{pmatrix} -4 \\\\ -2 \\end{pmatrix} = \\frac{1}{7}\\begin{pmatrix} -8 + 2 \\\\ 4 - 8 \\end{pmatrix} = \\frac{1}{7}\\begin{pmatrix} -6 \\\\ -4 \\end{pmatrix} = \\begin{pmatrix} -6/7 \\\\ -4/7 \\end{pmatrix}$$

判別スコアを計算する。各群の平均のスコアは：

$$Z_1 = \\mathbf{a}^\\top \\bar{\\mathbf{x}}_1 = -\\frac{6}{7}(2) - \\frac{4}{7}(3) = -\\frac{12}{7} - \\frac{12}{7} = -\\frac{24}{7} \\approx -3.43$$

$$Z_2 = \\mathbf{a}^\\top \\bar{\\mathbf{x}}_2 = -\\frac{6}{7}(6) - \\frac{4}{7}(5) = -\\frac{36}{7} - \\frac{20}{7} = -\\frac{56}{7} = -8.00$$

境界値（$\\pi_1 = \\pi_2$ なので中点）：

$$Z_0 = \\frac{Z_1 + Z_2}{2} = \\frac{-3.43 + (-8.00)}{2} = -5.71$$

新しい個体のスコア：

$$Z_0' = \\mathbf{a}^\\top \\mathbf{x}_0 = -\\frac{6}{7}(4) - \\frac{4}{7}(5) = -\\frac{24}{7} - \\frac{20}{7} = -\\frac{44}{7} \\approx -6.29$$

$Z_0' = -6.29 < Z_0 = -5.71$ なので、$Z_0'$ は $Z_2$ に近い。

したがって、$\\mathbf{x}_0 = (4, 5)^\\top$ はグループ2に判別される。
【/解答】

---

【例題】マハラノビス距離による判別を行え。

3群の判別問題で、各群の標本平均と共通共分散行列が以下で与えられている：

$$\\bar{\\mathbf{x}}_1 = \\begin{pmatrix} 1 \\\\ 2 \\end{pmatrix}, \\quad \\bar{\\mathbf{x}}_2 = \\begin{pmatrix} 4 \\\\ 3 \\end{pmatrix}, \\quad \\bar{\\mathbf{x}}_3 = \\begin{pmatrix} 3 \\\\ 6 \\end{pmatrix}$$

$$\\boldsymbol{S} = \\begin{pmatrix} 2 & 0.5 \\\\ 0.5 & 1 \\end{pmatrix}$$

事前確率は $\\pi_1 = 0.3$, $\\pi_2 = 0.5$, $\\pi_3 = 0.2$ とする。新しい個体 $\\mathbf{x}_0 = (3, 4)^\\top$ をマハラノビス距離に基づき判別せよ。

【解答】
逆行列を計算する：

$$\\boldsymbol{S}^{-1} = \\frac{1}{2 \\times 1 - 0.5^2}\\begin{pmatrix} 1 & -0.5 \\\\ -0.5 & 2 \\end{pmatrix} = \\frac{1}{1.75}\\begin{pmatrix} 1 & -0.5 \\\\ -0.5 & 2 \\end{pmatrix} = \\begin{pmatrix} 0.571 & -0.286 \\\\ -0.286 & 1.143 \\end{pmatrix}$$

各群からのマハラノビス距離を計算する。

**群1**: $\\mathbf{x}_0 - \\bar{\\mathbf{x}}_1 = (2, 2)^\\top$

$$D_1^2 = (2, 2) \\begin{pmatrix} 0.571 & -0.286 \\\\ -0.286 & 1.143 \\end{pmatrix} \\begin{pmatrix} 2 \\\\ 2 \\end{pmatrix} = (2, 2)\\begin{pmatrix} 0.571 \\\\ 1.714 \\end{pmatrix} = 1.142 + 3.428 = 4.571$$

**群2**: $\\mathbf{x}_0 - \\bar{\\mathbf{x}}_2 = (-1, 1)^\\top$

$$D_2^2 = (-1, 1) \\begin{pmatrix} 0.571 & -0.286 \\\\ -0.286 & 1.143 \\end{pmatrix} \\begin{pmatrix} -1 \\\\ 1 \\end{pmatrix} = (-1, 1)\\begin{pmatrix} -0.857 \\\\ 1.429 \\end{pmatrix} = 0.857 + 1.429 = 2.286$$

**群3**: $\\mathbf{x}_0 - \\bar{\\mathbf{x}}_3 = (0, -2)^\\top$

$$D_3^2 = (0, -2) \\begin{pmatrix} 0.571 & -0.286 \\\\ -0.286 & 1.143 \\end{pmatrix} \\begin{pmatrix} 0 \\\\ -2 \\end{pmatrix} = (0, -2)\\begin{pmatrix} 0.571 \\\\ -2.286 \\end{pmatrix} = 0 + 4.571 = 4.571$$

判別スコア関数（事前確率を含む）：

$$\\delta_g(\\mathbf{x}_0) = -\\frac{1}{2}D_g^2 + \\log \\pi_g$$

$$\\delta_1 = -\\frac{1}{2}(4.571) + \\log(0.3) = -2.286 - 1.204 = -3.490$$

$$\\delta_2 = -\\frac{1}{2}(2.286) + \\log(0.5) = -1.143 - 0.693 = -1.836$$

$$\\delta_3 = -\\frac{1}{2}(4.571) + \\log(0.2) = -2.286 - 1.609 = -3.895$$

$\\delta_2 = -1.836$ が最大なので、$\\mathbf{x}_0 = (3, 4)^\\top$ はグループ2に判別される。

マハラノビス距離のみでは群1と群3が同距離（$D^2 = 4.571$）だが、事前確率の差（$\\pi_1 = 0.3 > \\pi_3 = 0.2$）により群1が優先される。
【/解答】

---

【例題】LOOCVで誤判別率を推定せよ。

以下の8個のデータ（2次元、2群）に対し、Fisher の線形判別関数を適用し、LOOCV による誤判別率を推定せよ。

| 個体 | $X_1$ | $X_2$ | 群 |
|---|---|---|---|
| 1 | 1 | 2 | 1 |
| 2 | 2 | 3 | 1 |
| 3 | 3 | 2 | 1 |
| 4 | 2 | 1 | 1 |
| 5 | 5 | 5 | 2 |
| 6 | 6 | 4 | 2 |
| 7 | 5 | 6 | 2 |
| 8 | 7 | 5 | 2 |

【解答】
LOOCV では、$n = 8$ 回の判別を繰り返す。各回で1個体を除き、残り7個体で判別関数を構築し、除いた個体を判別する。

全データでの推定値を確認する。

群1の平均：$\\bar{\\mathbf{x}}_1 = (2, 2)^\\top$、群2の平均：$\\bar{\\mathbf{x}}_2 = (5.75, 5)^\\top$

各ステップで1個体を除いて平均と共分散を再計算し判別を行う。

例として個体1を除く場合（$i = 1$）：
- 群1の残りデータ：$(2,3), (3,2), (2,1)$ → 平均 $\\bar{\\mathbf{x}}_1^{(-1)} = (2.33, 2.00)^\\top$
- 群2のデータ：$(5,5), (6,4), (5,6), (7,5)$ → 平均 $\\bar{\\mathbf{x}}_2^{(-1)} = (5.75, 5.00)^\\top$
- 判別関数を再構築し、個体1の $(1, 2)$ を判別 → 群1に正しく判別

同様の手順を全8個体に対して繰り返す。2群の平均が十分離れている（$\\bar{\\mathbf{x}}_1 \\approx (2, 2)$, $\\bar{\\mathbf{x}}_2 \\approx (5.75, 5)$）ため、1個体の除外による影響は小さい。

全8回の判別を実施した結果、仮に全て正しく判別されたとすると：

$$\\mathrm{CV} = \\frac{0}{8} = 0$$

一方、再代入誤判別率も $\\mathrm{AER} = 0/8 = 0$ である。

この場合、2群は十分に分離されており、過適合の問題はない。一般に $\\mathrm{CV} \\geq \\mathrm{AER}$ であり、差が大きいほど過適合を示唆する。

注意：実際の試験問題では、境界付近のデータがあり誤判別が生じるケースが出題される。その場合は各 $i$ について丁寧に計算を行い、$I(\\hat{y}_{(i)} \\neq y_i)$ を集計する必要がある。
【/解答】`
    },
    {
      title: '演習問題',
      content: `**問題1**: Fisher の線形判別関数 $\\mathbf{a} = \\boldsymbol{\\Sigma}_W^{-1} (\\boldsymbol{\\mu}_1 - \\boldsymbol{\\mu}_2)$ が、グループ間分散とグループ内分散の比を最大化する理由を説明してください。

**問題2**: 2次元データで、グループ1の平均が $(0, 0)^\\top$、グループ2の平均が $(2, 1)^\\top$、共通共分散行列が $\\boldsymbol{\\Sigma} = \\begin{pmatrix} 1 & 0.5 \\\\ 0.5 & 1 \\end{pmatrix}$ である場合、判別係数を計算してください。

**問題3**: マハラノビス距離に基づく判別と Fisher の線形判別の関係を説明し、両者がいつ一致するかを述べてください。

**問題4**: Leave-One-Out 交差検証で得られた誤判別率が $0.15$、再代入誤判別率が $0.05$ である場合、何が示唆されるかを説明してください。

**問題5**: 3群（$n_1 = 20, n_2 = 30, n_3 = 25$）の3次元データに対する判別分析で、グループ間分散行列 $\\boldsymbol{\\Sigma}_B$ と グループ内分散行列 $\\boldsymbol{\\Sigma}_W$ が与えられたとき、判別関数の数は最大いくつか。また、各判別関数の判別力を評価するために用いる統計量を述べよ。

**問題6**: 二次判別分析（QDA）が線形判別分析（LDA）と異なる場面を説明せよ。各群の共分散行列が $\\boldsymbol{\\Sigma}_1 = \\begin{pmatrix} 4 & 0 \\\\ 0 & 1 \\end{pmatrix}$, $\\boldsymbol{\\Sigma}_2 = \\begin{pmatrix} 1 & 0 \\\\ 0 & 4 \\end{pmatrix}$ のとき、判別境界の形状がどうなるか論じよ。

**問題7**: ベイズ判別則において、誤判別のコスト（損失関数）が非対称な場合、判別境界がどのように変化するか。群1を群2に誤分類するコストが $c_{12} = 10$、群2を群1に誤分類するコストが $c_{21} = 1$ のとき、判別スコア関数への影響を式で示せ。

【例題】2群の線形判別関数を具体的に導出せよ。
2変量データ ($p = 2$) で2群のパラメータが以下のとおり与えられている。

$$\\boldsymbol{\\mu}_1 = \\begin{pmatrix} 1 \\\\ 3 \\end{pmatrix}, \\quad \\boldsymbol{\\mu}_2 = \\begin{pmatrix} 5 \\\\ 1 \\end{pmatrix}, \\quad \\boldsymbol{\\Sigma} = \\begin{pmatrix} 2 & 1 \\\\ 1 & 3 \\end{pmatrix}$$

線形判別関数 $\\delta_g(\\mathbf{x}) = \\boldsymbol{\\mu}_g^\\top \\boldsymbol{\\Sigma}^{-1}\\mathbf{x} - \\frac{1}{2}\\boldsymbol{\\mu}_g^\\top \\boldsymbol{\\Sigma}^{-1}\\boldsymbol{\\mu}_g$ を各群について具体的に求め、$\\mathbf{x} = (3, 2)^\\top$ を判別せよ。事前確率は等しいとする。

【解答】
$\\boldsymbol{\\Sigma}^{-1}$ を計算する。$|\\boldsymbol{\\Sigma}| = 6 - 1 = 5$ より：

$$\\boldsymbol{\\Sigma}^{-1} = \\frac{1}{5}\\begin{pmatrix} 3 & -1 \\\\ -1 & 2 \\end{pmatrix} = \\begin{pmatrix} 0.6 & -0.2 \\\\ -0.2 & 0.4 \\end{pmatrix}$$

群1の判別関数：

$$\\boldsymbol{\\mu}_1^\\top \\boldsymbol{\\Sigma}^{-1} = (1, 3)\\begin{pmatrix} 0.6 & -0.2 \\\\ -0.2 & 0.4 \\end{pmatrix} = (0.0, 1.0)$$

$$\\boldsymbol{\\mu}_1^\\top \\boldsymbol{\\Sigma}^{-1}\\boldsymbol{\\mu}_1 = (0.0, 1.0)\\begin{pmatrix} 1 \\\\ 3 \\end{pmatrix} = 3.0$$

$$\\delta_1(\\mathbf{x}) = 0.0 x_1 + 1.0 x_2 - \\frac{1}{2}(3.0) = x_2 - 1.5$$

群2の判別関数：

$$\\boldsymbol{\\mu}_2^\\top \\boldsymbol{\\Sigma}^{-1} = (5, 1)\\begin{pmatrix} 0.6 & -0.2 \\\\ -0.2 & 0.4 \\end{pmatrix} = (2.8, -0.6)$$

$$\\boldsymbol{\\mu}_2^\\top \\boldsymbol{\\Sigma}^{-1}\\boldsymbol{\\mu}_2 = (2.8, -0.6)\\begin{pmatrix} 5 \\\\ 1 \\end{pmatrix} = 13.4$$

$$\\delta_2(\\mathbf{x}) = 2.8 x_1 - 0.6 x_2 - \\frac{1}{2}(13.4) = 2.8 x_1 - 0.6 x_2 - 6.7$$

$\\mathbf{x} = (3, 2)^\\top$ に対して：

$$\\delta_1(3, 2) = 2.0 - 1.5 = 0.5$$

$$\\delta_2(3, 2) = 2.8(3) - 0.6(2) - 6.7 = 8.4 - 1.2 - 6.7 = 0.5$$

$\\delta_1 = \\delta_2 = 0.5$ であるから、$\\mathbf{x} = (3, 2)^\\top$ は判別境界上にある。この点はどちらの群にも等確率で所属する。
【/解答】

【例題】等分散仮定の下での判別境界を求めよ。
1次元データ ($p = 1$) で2群の母平均が $\\mu_1 = 10$, $\\mu_2 = 20$, 共通分散が $\\sigma^2 = 25$ である。事前確率が等しいとき、判別境界を求めよ。また、事前確率が $\\pi_1 = 0.7$, $\\pi_2 = 0.3$ の場合、境界がどう変化するか計算せよ。

【解答】
等分散仮定の下で事前確率が等しい場合、判別境界は2群の平均の中点：

$$x_0 = \\frac{\\mu_1 + \\mu_2}{2} = \\frac{10 + 20}{2} = 15$$

$x > 15$ なら群2、$x \\leq 15$ なら群1に判別する。

事前確率が $\\pi_1 = 0.7$, $\\pi_2 = 0.3$ の場合、判別スコア関数は：

$$\\delta_g(x) = \\frac{\\mu_g}{\\sigma^2}x - \\frac{\\mu_g^2}{2\\sigma^2} + \\log \\pi_g$$

境界は $\\delta_1(x_0) = \\delta_2(x_0)$ を解く：

$$\\frac{\\mu_1}{\\sigma^2}x_0 - \\frac{\\mu_1^2}{2\\sigma^2} + \\log \\pi_1 = \\frac{\\mu_2}{\\sigma^2}x_0 - \\frac{\\mu_2^2}{2\\sigma^2} + \\log \\pi_2$$

$$\\frac{\\mu_1 - \\mu_2}{\\sigma^2}x_0 = \\frac{\\mu_1^2 - \\mu_2^2}{2\\sigma^2} + \\log \\pi_2 - \\log \\pi_1$$

$$\\frac{-10}{25}x_0 = \\frac{100 - 400}{50} + \\log\\frac{0.3}{0.7}$$

$$-0.4 x_0 = -6 + (-0.8473)$$

$$x_0 = \\frac{6.8473}{0.4} = 17.12$$

事前確率が群1に有利（$\\pi_1 > \\pi_2$）であるため、判別境界は群2の平均側にシフトし、群1に判別される領域が広がった（$15 \\to 17.12$）。
【/解答】

【例題】事前確率を考慮したベイズ判別を行え。
2変量データで2群のパラメータが以下のとおりである。

$$\\bar{\\mathbf{x}}_1 = \\begin{pmatrix} 0 \\\\ 0 \\end{pmatrix}, \\quad \\bar{\\mathbf{x}}_2 = \\begin{pmatrix} 4 \\\\ 4 \\end{pmatrix}, \\quad \\mathbf{S}_W = \\begin{pmatrix} 1 & 0 \\\\ 0 & 1 \\end{pmatrix}$$

事前確率 $\\pi_1 = 0.8$, $\\pi_2 = 0.2$ のとき、$\\mathbf{x}_0 = (2, 2)^\\top$ をベイズ判別せよ。

【解答】
$\\mathbf{S}_W = \\mathbf{I}$ のため、マハラノビス距離はユークリッド距離に一致する。

$$D_1^2 = (2-0)^2 + (2-0)^2 = 8$$

$$D_2^2 = (2-4)^2 + (2-4)^2 = 8$$

マハラノビス距離は同じだが、事前確率が異なる。判別スコア関数：

$$\\delta_1(\\mathbf{x}_0) = -\\frac{1}{2}D_1^2 + \\log \\pi_1 = -4 + \\log 0.8 = -4 + (-0.2231) = -4.223$$

$$\\delta_2(\\mathbf{x}_0) = -\\frac{1}{2}D_2^2 + \\log \\pi_2 = -4 + \\log 0.2 = -4 + (-1.6094) = -5.609$$

$\\delta_1 = -4.223 > \\delta_2 = -5.609$ より、$\\mathbf{x}_0$ は群1に判別される。

事後確率を計算すると：

$$P(G=1|\\mathbf{x}_0) = \\frac{\\pi_1 \\exp(-D_1^2/2)}{\\pi_1 \\exp(-D_1^2/2) + \\pi_2 \\exp(-D_2^2/2)} = \\frac{0.8 \\times e^{-4}}{0.8 \\times e^{-4} + 0.2 \\times e^{-4}} = \\frac{0.8}{0.8 + 0.2} = 0.8$$

距離が等しいため、事後確率は事前確率そのものとなる。事前確率を考慮しなければ両群に等距離で判別不能だが、$\\pi_1 = 0.8$ の情報により群1に判別される。
【/解答】

【例題】二次判別分析（QDA）を実行せよ。
2群の共分散行列が異なる場合を考える。$p = 1$（1次元）で以下のパラメータが与えられている。

$$\\mu_1 = 0, \\; \\sigma_1^2 = 1, \\; \\mu_2 = 3, \\; \\sigma_2^2 = 4, \\; \\pi_1 = \\pi_2 = 0.5$$

QDAの判別スコア関数を書き、判別境界を求めよ。

【解答】
群 $g$ のQDA判別スコア関数は：

$$\\delta_g(x) = -\\frac{1}{2}\\log|\\sigma_g^2| - \\frac{1}{2}\\frac{(x - \\mu_g)^2}{\\sigma_g^2} + \\log \\pi_g$$

群1：

$$\\delta_1(x) = -\\frac{1}{2}\\log 1 - \\frac{x^2}{2} + \\log 0.5 = -\\frac{x^2}{2} - 0.693$$

群2：

$$\\delta_2(x) = -\\frac{1}{2}\\log 4 - \\frac{(x-3)^2}{8} + \\log 0.5 = -0.693 - \\frac{(x-3)^2}{8} - 0.693$$

$$= -\\frac{(x-3)^2}{8} - 1.386$$

判別境界は $\\delta_1(x) = \\delta_2(x)$ を解く：

$$-\\frac{x^2}{2} - 0.693 = -\\frac{(x-3)^2}{8} - 1.386$$

$$-\\frac{x^2}{2} + \\frac{(x-3)^2}{8} = -0.693$$

$$-\\frac{4x^2}{8} + \\frac{x^2 - 6x + 9}{8} = -0.693$$

$$\\frac{-3x^2 - 6x + 9}{8} = -0.693$$

$$-3x^2 - 6x + 9 = -5.544$$

$$3x^2 + 6x - 14.544 = 0$$

$$x = \\frac{-6 \\pm \\sqrt{36 + 174.5}}{6} = \\frac{-6 \\pm \\sqrt{210.5}}{6} = \\frac{-6 \\pm 14.51}{6}$$

$$x_1 = \\frac{8.51}{6} \\approx 1.418, \\quad x_2 = \\frac{-20.51}{6} \\approx -3.418$$

判別境界が2つ存在する。$x \\in (-3.42, 1.42)$ では群1、それ以外では群2に判別される。LDAでは境界が直線（1点）だが、QDAでは共分散行列の違いにより二次曲線（ここでは2つの境界点）となる。
【/解答】

【例題】交差検証による誤判別率の推定を行え。
以下の6個のデータ（1次元、2群）に対して5分割交差検証（$k = 3$, 各分割2個体）を用いて誤判別率を推定せよ。判別境界は2群の平均の中点とする。

| 個体 | $X$ | 群 |
|---|---|---|
| 1 | 1.0 | 1 |
| 2 | 2.5 | 1 |
| 3 | 3.0 | 1 |
| 4 | 4.5 | 2 |
| 5 | 5.0 | 2 |
| 6 | 7.0 | 2 |

分割：$\\{1,4\\}$, $\\{2,5\\}$, $\\{3,6\\}$

【解答】
各分割をテストデータとし、残りを訓練データとする。

**分割1**（テスト：個体1,4、訓練：個体2,3,5,6）：
訓練データの群平均：$\\bar{x}_1 = (2.5 + 3.0)/2 = 2.75$, $\\bar{x}_2 = (5.0 + 7.0)/2 = 6.0$
判別境界：$(2.75 + 6.0)/2 = 4.375$
個体1（$X = 1.0$）：$1.0 < 4.375$ → 群1に判別（正解）
個体4（$X = 4.5$）：$4.5 > 4.375$ → 群2に判別（正解）
誤判別数：0

**分割2**（テスト：個体2,5、訓練：個体1,3,4,6）：
訓練データの群平均：$\\bar{x}_1 = (1.0 + 3.0)/2 = 2.0$, $\\bar{x}_2 = (4.5 + 7.0)/2 = 5.75$
判別境界：$(2.0 + 5.75)/2 = 3.875$
個体2（$X = 2.5$）：$2.5 < 3.875$ → 群1に判別（正解）
個体5（$X = 5.0$）：$5.0 > 3.875$ → 群2に判別（正解）
誤判別数：0

**分割3**（テスト：個体3,6、訓練：個体1,2,4,5）：
訓練データの群平均：$\\bar{x}_1 = (1.0 + 2.5)/2 = 1.75$, $\\bar{x}_2 = (4.5 + 5.0)/2 = 4.75$
判別境界：$(1.75 + 4.75)/2 = 3.25$
個体3（$X = 3.0$）：$3.0 < 3.25$ → 群1に判別（正解）
個体6（$X = 7.0$）：$7.0 > 3.25$ → 群2に判別（正解）
誤判別数：0

$$\\text{CV誤判別率} = \\frac{0 + 0 + 0}{6} = 0$$

2群は十分に分離されているため、交差検証でも誤判別は生じなかった。再代入誤判別率も0であり、過適合の兆候はない。
【/解答】

【例題】3群の判別分析における分類規則を導出し適用せよ。
3群（$g = 1, 2, 3$）の2変量データで、各群の平均と共通共分散行列が以下のとおりである。

$$\\bar{\\mathbf{x}}_1 = \\begin{pmatrix} 0 \\\\ 0 \\end{pmatrix}, \\quad \\bar{\\mathbf{x}}_2 = \\begin{pmatrix} 4 \\\\ 0 \\end{pmatrix}, \\quad \\bar{\\mathbf{x}}_3 = \\begin{pmatrix} 2 \\\\ 4 \\end{pmatrix}, \\quad \\mathbf{S}_W = \\begin{pmatrix} 2 & 0 \\\\ 0 & 2 \\end{pmatrix}$$

事前確率を $\\pi_1 = \\pi_2 = \\pi_3 = 1/3$ とする。線形判別スコア関数 $\\delta_g(\\mathbf{x})$ を各群について求め、$\\mathbf{x}_0 = (2, 2)^\\top$ を分類せよ。

【解答】
$\\mathbf{S}_W^{-1} = \\frac{1}{2}\\mathbf{I}_2$ である。線形判別スコア関数は：

$$\\delta_g(\\mathbf{x}) = \\bar{\\mathbf{x}}_g^\\top \\mathbf{S}_W^{-1} \\mathbf{x} - \\frac{1}{2}\\bar{\\mathbf{x}}_g^\\top \\mathbf{S}_W^{-1} \\bar{\\mathbf{x}}_g + \\log \\pi_g$$

事前確率が等しいので $\\log \\pi_g$ の項は共通で省略可能。

群1：$\\bar{\\mathbf{x}}_1^\\top \\mathbf{S}_W^{-1} = (0, 0) \\cdot \\frac{1}{2}\\mathbf{I} = (0, 0)$

$$\\delta_1(\\mathbf{x}) = 0 \\cdot x_1 + 0 \\cdot x_2 - 0 = 0$$

群2：$\\bar{\\mathbf{x}}_2^\\top \\mathbf{S}_W^{-1} = (4, 0) \\cdot \\frac{1}{2}\\mathbf{I} = (2, 0)$

$$\\delta_2(\\mathbf{x}) = 2x_1 - \\frac{1}{2}(2)(4) = 2x_1 - 4$$

群3：$\\bar{\\mathbf{x}}_3^\\top \\mathbf{S}_W^{-1} = (2, 4) \\cdot \\frac{1}{2}\\mathbf{I} = (1, 2)$

$$\\delta_3(\\mathbf{x}) = x_1 + 2x_2 - \\frac{1}{2}(1 \\cdot 2 + 2 \\cdot 4) = x_1 + 2x_2 - 5$$

$\\mathbf{x}_0 = (2, 2)^\\top$ に対して：

$$\\delta_1(2,2) = 0, \\quad \\delta_2(2,2) = 4 - 4 = 0, \\quad \\delta_3(2,2) = 2 + 4 - 5 = 1$$

$\\delta_3 = 1$ が最大であるから、$\\mathbf{x}_0$ は群3に分類される。3群の判別では $\\min(3-1, 2) = 2$ 個の判別関数が構成され、2次元の判別空間で各群の領域がボロノイ図的に区分される。
【/解答】

【例題】正則化判別分析（RDA）の概念を説明し、正則化パラメータの効果を計算せよ。
2群の判別分析で、群1の標本サイズが $n_1 = 5$、群2の標本サイズが $n_2 = 5$ であり、$p = 4$ 次元のデータである。プール共分散行列の推定値が以下のとおりである。

$$\\mathbf{S}_W = \\begin{pmatrix} 3 & 1 & 0.5 & 0.2 \\\\ 1 & 4 & 0.8 & 0.3 \\\\ 0.5 & 0.8 & 2 & 0.6 \\\\ 0.2 & 0.3 & 0.6 & 1 \\end{pmatrix}$$

正則化パラメータ $\\alpha = 0.5$ を用いた正則化共分散行列 $\\mathbf{S}_{\\text{RDA}}(\\alpha)$ を定義し、その効果を論じよ。

【解答】
標本サイズ $n = n_1 + n_2 = 10$ に対して $p = 4$ であり、$n - 2 = 8$ と $p$ が近いため、$\\mathbf{S}_W$ の推定は不安定になりやすい。正則化判別分析（RDA）では、共分散行列を以下のように正則化する：

$$\\mathbf{S}_{\\text{RDA}}(\\alpha) = (1 - \\alpha)\\mathbf{S}_W + \\alpha \\cdot \\text{diag}(\\mathbf{S}_W)$$

ここで $\\text{diag}(\\mathbf{S}_W)$ は $\\mathbf{S}_W$ の対角成分のみを残した行列であり、$\\alpha \\in [0, 1]$ は正則化パラメータである。

$\\alpha = 0$ のとき通常のLDA、$\\alpha = 1$ のとき対角共分散（ナイーブベイズ的）となる。

$\\alpha = 0.5$ のとき：

$$\\mathbf{S}_{\\text{RDA}}(0.5) = 0.5 \\begin{pmatrix} 3 & 1 & 0.5 & 0.2 \\\\ 1 & 4 & 0.8 & 0.3 \\\\ 0.5 & 0.8 & 2 & 0.6 \\\\ 0.2 & 0.3 & 0.6 & 1 \\end{pmatrix} + 0.5 \\begin{pmatrix} 3 & 0 & 0 & 0 \\\\ 0 & 4 & 0 & 0 \\\\ 0 & 0 & 2 & 0 \\\\ 0 & 0 & 0 & 1 \\end{pmatrix}$$

$$= \\begin{pmatrix} 3 & 0.5 & 0.25 & 0.1 \\\\ 0.5 & 4 & 0.4 & 0.15 \\\\ 0.25 & 0.4 & 2 & 0.3 \\\\ 0.1 & 0.15 & 0.3 & 1 \\end{pmatrix}$$

非対角成分が元の半分に縮小されており、行列の条件数が改善される。$\\mathbf{S}_W$ の逆行列が不安定な場合でも $\\mathbf{S}_{\\text{RDA}}(0.5)$ は対角優位性が増し、安定した逆行列計算が可能となる。

最適な $\\alpha$ は交差検証による誤判別率の最小化で選択する。小標本・高次元（$p$ が $n$ に比べて大きい）の状況で特に有効であり、LDAとQDAの中間的な柔軟性を持つ判別法として実用的である。
【/解答】`
    }
  ]
};
