import { Brain } from 'lucide-react';
export const topicBayes = {
  id: 'bayes',
  name: 'ベイズ法',
  icon: Brain,
  description: '事前分布と尤度から事後分布を導出する統計推論とMCMC法の実装',
  sections: [
    {
      title: 'ベイズ推論の基礎',
      content: `**ベイズの定理**
母数θ、データXの尤度関数L(θ|X)と事前分布π(θ)から、事後分布π(θ|X)を計算します：
  π(θ|X) = (L(θ|X) × π(θ)) / ∫ L(θ|X) × π(θ) dθ
          ∝ L(θ|X) × π(θ)

分母 p(X) = ∫ L(θ|X) × π(θ) dθ を周辺尤度（エビデンス）と呼びます。

**比例形式での表現**
計算上、正規化定数は不要なことが多いため：
  事後分布 ∝ 尤度関数 × 事前分布
  π(θ|X) ∝ L(θ|X) × π(θ)

**確率密度の解釈**
- π(θ)：事前分布。データ観察前の母数に対する信念
- L(θ|X)：尤度関数。データを観察した下での母数の相対的尤度
- π(θ|X)：事後分布。データを観察した後の母数の更新された信念

**例：コイントスの偏りの推定**
表が出る確率θについて：
- 事前信念：β分布 π(θ) = Beta(1,1)（無情報事前分布）
- データ：n=10回中、表が7回
- 尤度：L(θ|X) ∝ θ⁷(1-θ)³
- 事後分布：π(θ|X) ∝ θ⁷(1-θ)³ × 1 = θ⁷(1-θ)³
  これは Beta(8, 4) に相当

**逐次的な更新**
ベイズ推論は逐次的に行える（オンラインラーニング）：
- 新しいデータが得られたら、旧事後分布を新事前分布として利用
- 新データの尤度と掛け合わせて新事後分布を得る
  π_new(θ|X₁,X₂) ∝ L(θ|X₂) × π_old(θ|X₁)

**例：疾病検査の事後確率**
問題：ある疾病の有病率5%です。検査精度は感度99%、特異度95%。陽性反応が出た場合、実際に患者である確率は？
解答：P(病気|陽性)∝ P(陽|病)×P(病)= 0.99×0.05=0.0495、P(病気|陽性)=0.0495/(0.0495+0.05×0.05)≈0.665（67%）`
    },
    {
      title: '共役事前分布',
      content: `**共役性の定義**
事前分布π(θ)と尤度L(θ|X)が「共役」であるとは、事後分布π(θ|X)が事前分布と同じ家族に属することです。

利点：
- 事後分布の解析的形式が得られる（計算効率化）
- パラメータの更新ルールが単純
- 逐次学習が容易

**Beta-Binomial共役性**
事前分布：π(p) = Beta(α, β)
尤度：L(p|X) = p^k(1-p)^{n-k}（n回中k回成功）
事後分布：π(p|X) = Beta(α+k, β+n-k)

解釈：
- α, β は事前の「擬似観測」を表す
- α個の「成功」と β個の「失敗」を事前に観測したと解釈
- データk成功、(n-k)失敗が加わり、新パラメータ(α+k, β+n-k)が得られる

**Normal-Normal共役性（既知分散）**
事前分布：π(μ) = N(μ₀, σ₀²)
尤度：L(μ|X) ∝ exp(-Σ(Xᵢ-μ)² / (2σ²))（既知分散σ²）
事後分布：π(μ|X) = N(μ₁, σ₁²)

ここで：
  1/σ₁² = 1/σ₀² + n/σ²
  μ₁ = σ₁²(μ₀/σ₀² + nX̄/σ²)

または重み付け表示：
  μ₁ = w × X̄ + (1-w) × μ₀，ここで w = (n/σ²) / (1/σ₀² + n/σ²)

**Gamma-Poisson共役性**
事前分布：π(λ) = Gamma(α, β)
尤度：L(λ|X) = λ^{Σxᵢ} e^{-nλ}（Poissonサンプル）
事後分布：π(λ|X) = Gamma(α + Σxᵢ, β + n)

**共役事前分布の表**
尤度分布        | 共役事前分布      | 事後分布パラメータ
----------------|------------------|--------------------
Binomial(p)    | Beta(α,β)       | Beta(α+k, β+n-k)
Poisson(λ)     | Gamma(α,β)      | Gamma(α+Σxᵢ, β+n)
Normal(μ,σ²)  | N(μ₀,σ₀²)      | N(μ₁, σ₁²)
Exponential(λ) | Gamma(α,β)      | Gamma(α+n, β+Σxᵢ)

**例：Beta-Binomial逐次更新**
問題：事前：Beta(1,1)、実験1：3成功7失敗、実験2：5成功5失敗。最終事後分布は？
解答：実験1後：Beta(1+3, 1+7)=Beta(4,8)。実験2後：Beta(4+5, 8+5)=Beta(9,13)`
    },
    {
      title: 'ベイズ推定',
      content: `**点推定法（Point Estimation）**
事後分布から単一の値を推定する方法：

1. **最大事後推定量（MAP: Maximum A Posteriori）**
  θ̂_MAP = argmax_θ π(θ|X)
事後分布の最頻値。
利点：計算が比較的簡単
欠点：非対称な分布では中心傾向が不十分

2. **事後平均（Posterior Mean）**
  θ̂_mean = E[θ|X] = ∫ θ × π(θ|X) dθ
事後分布の期待値。
利点：二乗誤差損失の下で最適
欠点：計算がやや複雑

3. **事後中央値（Posterior Median）**
  θ̂_median : P(θ < θ̂_median | X) = 0.5
絶対誤差損失の下で最適。外れ値に強い。

**信用区間（Credible Interval）**
事後分布から100(1-α)%信用区間を構成：
  P(θ_L < θ < θ_U | X) = 1 - α

**最高密度領域（HPD: Highest Posterior Density）**
  θ_L = inf{θ : π(θ|X) ≥ c}, θ_U = sup{θ : π(θ|X) ≥ c}
で定義される区間で、最短の信用区間。

対称事後分布では等尾信用区間と一致。

**例：Betaの推定**
事後分布π(p|X) = Beta(9, 5)の場合：
  MAP = (9-1)/(9+5-2) = 8/12 = 2/3
  E[p|X] = 9/(9+5) = 9/14 ≈ 0.643
  90%信用区間 = [0.42, 0.83]（Beta分布の分位点から）

**予測分布（Predictive Distribution）**
将来のデータy_futureの分布：
  p(y_future|X) = ∫ p(y_future|θ) × π(θ|X) dθ
事後分布で尤度を積分。パラメータの不確実性を組み込む。

例：Poisson-Gamma の場合、予測分布は負の二項分布になります。

**例：信用区間の計算**
問題：事後分布 Beta(4, 8)から90%信用区間を求めてください。
解答：Beta(4,8)の0.05分位数≈0.17、0.95分位数≈0.48。90%信用区間=[0.17, 0.48]`
    },
    {
      title: 'MCMC法',
      content: `**概要**
解析的に積分が計算できない高次元問題の事後分布からサンプリングする手法。マルコフ連鎖モンテカルロ（Markov Chain Monte Carlo）です。

**Metropolis-Hastings法**
提案分布q(θ*|θ)から候補を生成し、確率で受理/棄却して、事後分布に従うサンプルを得ます。

アルゴリズム（反復t）：
1. 現在値θ^(t)から提案分布q(θ*|θ^(t))で候補θ*を生成
2. 受理確率を計算：
     α = min(1, [π(θ*|X)/π(θ^(t)|X)] × [q(θ^(t)|θ*)/q(θ*|θ^(t))])
3. U ~ Uniform(0,1)なら：
   - U < α のとき、θ^(t+1) = θ* (受理)
   - U ≥ α のとき、θ^(t+1) = θ^(t) (棄却、値を繰り返す)

特殊な場合：対称提案分布q(θ*|θ)=q(θ|θ*)のとき、
  α = min(1, π(θ*|X) / π(θ^(t)|X))

**Gibbs抽出法**
多変量問題でθ = (θ₁, θ₂, ..., θₚ)の場合、条件付き分布から順番にサンプリング：

アルゴリズム：
1. θ₁^(t+1) ~ π(θ₁ | θ₂^(t), ..., θₚ^(t), X)
2. θ₂^(t+1) ~ π(θ₂ | θ₁^(t+1), θ₃^(t), ..., θₚ^(t), X)
3. ... (各成分をこの順で更新)
4. θₚ^(t+1) ~ π(θₚ | θ₁^(t+1), ..., θₚ₋₁^(t+1), X)

利点：複雑な高次元問題に効果的、受理棄却がない

**収束診断（Convergence Diagnostics）**
MCMC出力の有効性を確認：

1. **トレースプロット**：反復図。安定し、水平に見える
2. **自己相関関数（ACF）**：ラグが小さいほど効率的
3. **Gelman-Rubin統計量（R̂）**：複数チェーンで R̂ < 1.1 が目安
   R̂ = √[(n-1+B/nW)/B] < 1.1 で収束と判定
   （B：チェーン間分散、W：チェーン内分散）

**バーンイン期間（Burn-in）**
初期値の影響を除去するため、最初の反復を捨てます。
- 通常、全体の10%-50%をバーンインとして指定
- トレースプロットで安定後のサンプルを使用

**例：ベイズロジスティック回帰**
応答変数Y、説明変数X、パラメータβに対するMCMC：
1. 提案分布で β* を生成
2. 受理確率計算：α = min(1, L(β*|Y,X)π(β*) / L(β^(t)|Y,X)π(β^(t)))
3. サンプルを蓄積して事後分布を推定

**例：簡単なMH法の実行**
問題：1次元問題で現在値θ=2、提案値θ*=2.5、事後比π(θ*|X)/π(θ^(t)|X)=0.8の場合、受理確率と次の値を決定してください。
解答：α=min(1, 0.8)=0.8。U~U(0,1)で、U<0.8なら受理（θ^(t+1)=2.5）、U≥0.8なら棄却（θ^(t+1)=2）`
    },
    {
      title: '演習問題',
      content: `**問題1：ベイズの定理の計算**
製造過程で、不良品率θの2つの仮説：
- H₁：θ=0.01（良好）、事前確率P(H₁)=0.9
- H₂：θ=0.1（不調）、事前確率P(H₂)=0.1

サンプル100個中不良品2個が発見。各仮説の事後確率を計算してください。

**問題2：共役事前分布**
Poisson(λ)データに対し、事前分布 Gamma(2,0.5) を設定。
データ：n=10, Σxᵢ=15

(1) 事後分布のパラメータは？
(2) 事後平均 E[λ|X] は？
(3) 次の観測値Y_future の予測分布は何か？

**問題3：Beta-Binomial逐次更新**
無情報事前分布Beta(1,1)から開始。
- 実験1：20回中14回成功
- 実験2：15回中12回成功

各段階での事後分布を求め、成功確率pの点推定（MAP、平均）を計算してください。

**問題4：信用区間の構成**
事後分布 N(μ=75, σ²=4) （データから平均と分散を推定）

(1) 95%信用区間を求めてください。
(2) 頻度主義の信頼区間とベイズ信用区間の違いを説明してください。

**問題5：予測分布**
事後分布が Beta(8, 4) のとき、

(1) 次の1回の観測Y_future=1（成功）の確率は？
(2) Beta-Binomial予測分布の平均と分散は？

**問題6：MCMC診断**
Metropolis-Hastings法を1000反復実行後、トレースプロットで：
- 最初の100反復で不安定な変動
- 100反復以降、安定して特定値の周辺で変動
- ACFでlag=50で自己相関≈0.1

(1) バーンイン期間はいくつが適切ですか？
(2) 実効サンプルサイズ（ESS）は何を示していますか？

**問題7：Gibbs抽出**
二変量正規分布で条件付き分布が既知の場合、Gibbs法のアルゴリズムを記述してください。
Metropolis-Hastings法との利点・欠点を比較してください。`
    }
  ]
};
