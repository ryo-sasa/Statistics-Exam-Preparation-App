import { PieChart } from 'lucide-react';

export const topicIncomplete = {
  id: 'incomplete',
  name: '不完全データの統計処理',
  icon: PieChart,
  description: '欠測メカニズムとEM アルゴリズム',
  sections: [
    {
      title: '欠測のメカニズム',
      content: `**MCAR（完全にランダムに欠測）**
欠測の生起がデータの観測値にも未観測値にも依存しない場合、欠測はMCARです。数式で表すと：

$$P(R | Y_{\\text{obs}}, Y_{\\text{mis}}) = P(R)$$

ここで $R$ は欠測指示変数です。この場合、欠測メカニズムは無視可能（ignorable）であり、欠測パターンを気にせず母数推定ができます。

**MAR（ランダムに欠測）**
欠測の生起が観測値には依存するが、未観測値には依存しない場合、欠測はMARです：

$$P(R | Y_{\\text{obs}}, Y_{\\text{mis}}) = P(R | Y_{\\text{obs}})$$

MARでは、観測データのみを利用した尤度関数は一致推定量を提供します。ただし単純な方法（リストワイズ削除など）は誤りを導く可能性があります。

**MNAR（非ランダムに欠測）**
欠測の生起が未観測値に依存する場合、欠測はMNARです：

$$P(R | Y_{\\text{obs}}, Y_{\\text{mis}}) \\text{ は } Y_{\\text{mis}} \\text{ に依存}$$

MNARでは、欠測メカニズムのモデル化が必須です。例えば、給与が高い人ほど給与を報告しない場合です。この場合、欠測パターンの情報なくしては正確な推定が困難です。`
    },
    {
      title: 'EM アルゴリズム',
      content: `**EM アルゴリズムの概要**
EM（Expectation-Maximization）アルゴリズムは、欠測を含むデータから最尤推定量を求めるための反復法です。観測データを $Y_{\\text{obs}}$、欠測データを $Y_{\\text{mis}}$、パラメータを $\\theta$ とするとき、次のように進みます：

**E-ステップ（Expectation）**
パラメータ $\\theta^{(t)}$ の現在値の下で、欠測データの条件付き期待値を計算します：

$$Q(\\theta | \\theta^{(t)}) = E\\left[\\log L(\\theta; Y_{\\text{obs}}, Y_{\\text{mis}}) \\mid Y_{\\text{obs}}, \\theta^{(t)}\\right]$$

ここで $L$ は完全尤度関数です。このステップでは欠測データを「補完」する期待値を計算します。

**M-ステップ（Maximization）**
$Q$ 関数を $\\theta$ について最大化し、新しいパラメータ推定値を得ます：

$$\\theta^{(t+1)} = \\arg\\max_{\\theta} Q(\\theta | \\theta^{(t)})$$

**対数尤度の単調増加**
EMアルゴリズムの重要な性質として、観測尤度 $L_{\\text{obs}}(\\theta; Y_{\\text{obs}})$ は各反復で単調に増加します：

$$\\log L_{\\text{obs}}(\\theta^{(t+1)}; Y_{\\text{obs}}) \\geq \\log L_{\\text{obs}}(\\theta^{(t)}; Y_{\\text{obs}})$$

これにより、アルゴリズムは観測尤度の局所最大値に収束することが保証されます。反復は収束判定基準（例えば $\\|\\theta^{(t+1)} - \\theta^{(t)}\\| < \\varepsilon$）で終了します。`
    },
    {
      title: '具体例：正規混合分布の EM',
      content: `**問題設定**
データ $Y_1, \\ldots, Y_n$ が2つの正規分布 $N(\\mu_1, \\sigma_1^2)$ と $N(\\mu_2, \\sigma_2^2)$ の混合から生成されるとします。混合比を $\\pi_1, \\; \\pi_2 = 1 - \\pi_1$ とし、各データがどちらの分布から生成されたかを表す潜在変数 $Z_i \\in \\{1, 2\\}$ が欠測です。

**E-ステップ**
パラメータ $\\theta = (\\mu_1, \\sigma_1, \\mu_2, \\sigma_2, \\pi_1)$ の現在値の下で、各データが成分 $j$ から生成された確率（負担率）を計算します：

$$r_{ij} = \\frac{\\pi_j \\, \\phi(Y_i; \\mu_j, \\sigma_j^2)}{\\sum_{k=1}^{2} \\pi_k \\, \\phi(Y_i; \\mu_k, \\sigma_k^2)}$$

ここで $\\phi$ は正規分布の確率密度関数です。

**M-ステップ**
新しいパラメータを計算します：

$$\\pi_1^{(t+1)} = \\frac{1}{n} \\sum_{i=1}^{n} r_{i1}$$

$$\\mu_1^{(t+1)} = \\frac{\\sum_{i=1}^{n} r_{i1} Y_i}{\\sum_{i=1}^{n} r_{i1}}$$

$$\\sigma_1^{2\\,(t+1)} = \\frac{\\sum_{i=1}^{n} r_{i1} (Y_i - \\mu_1^{(t+1)})^2}{\\sum_{i=1}^{n} r_{i1}}$$

同様に $\\mu_2, \\sigma_2, \\pi_2$ も計算します。

**実装のポイント**
アルゴリズムは数回の反復で収束します。対数尤度

$$\\ell = \\sum_{i=1}^{n} \\log\\left(\\pi_1 \\, \\phi(Y_i; \\mu_1, \\sigma_1^2) + \\pi_2 \\, \\phi(Y_i; \\mu_2, \\sigma_2^2)\\right)$$

が増加していることを確認しながら実装します。`
    },
    {
      title: '演習問題',
      content: `**問題1**：MCAR、MAR、MNARの違いを説明し、それぞれの例を3つずつ挙げてください。

**問題2**：EMアルゴリズムのE-ステップとM-ステップの目的を説明し、なぜこの2つのステップを交互に繰り返すことで最尤推定量を求められるのかを論述してください。

**問題3**：正規混合分布のEMアルゴリズムにおいて、$r_{ij} = \\frac{\\pi_j \\, \\phi(Y_i; \\mu_j, \\sigma_j^2)}{\\sum_k \\pi_k \\, \\phi(Y_i; \\mu_k, \\sigma_k^2)}$ が何を表しているかを説明してください。

**問題4**：EMアルゴリズムの収束性について、観測尤度の単調増加性がなぜ重要なのかを説明してください。

【例題】正規混合分布のEMアルゴリズムのE-stepとM-stepを実行せよ
2成分正規混合分布を考える。現在のパラメータ推定値が $\\mu_1^{(t)} = 0$, $\\mu_2^{(t)} = 5$, $\\sigma_1^{(t)} = \\sigma_2^{(t)} = 1$, $\\pi_1^{(t)} = 0.5$ である。データ $Y = \\{1, 2, 4, 6\\}$ に対してE-stepで負担率 $r_{i1}$ を計算し、M-stepで $\\mu_1^{(t+1)}$ と $\\pi_1^{(t+1)}$ を求めよ。

【解答】
E-step：各データ点について $r_{i1}$ を計算する。$\\pi_1 = \\pi_2 = 0.5$ なので：

$$r_{i1} = \\frac{\\phi(Y_i; 0, 1)}{\\phi(Y_i; 0, 1) + \\phi(Y_i; 5, 1)}$$

$Y_1 = 1$：$\\phi(1; 0, 1) = 0.2420$, $\\phi(1; 5, 1) = 0.0001$ より $r_{11} = \\frac{0.2420}{0.2421} \\approx 0.9996$

$Y_2 = 2$：$\\phi(2; 0, 1) = 0.0540$, $\\phi(2; 5, 1) = 0.0044$ より $r_{21} = \\frac{0.0540}{0.0584} \\approx 0.9247$

$Y_3 = 4$：$\\phi(4; 0, 1) = 0.0001$, $\\phi(4; 5, 1) = 0.2420$ より $r_{31} = \\frac{0.0001}{0.2421} \\approx 0.0004$

$Y_4 = 6$：$\\phi(6; 0, 1) \\approx 0$, $\\phi(6; 5, 1) = 0.2420$ より $r_{41} \\approx 0$

M-step：

$$\\pi_1^{(t+1)} = \\frac{1}{4}(0.9996 + 0.9247 + 0.0004 + 0) \\approx 0.481$$

$$\\mu_1^{(t+1)} = \\frac{0.9996 \\times 1 + 0.9247 \\times 2 + 0.0004 \\times 4 + 0 \\times 6}{0.9996 + 0.9247 + 0.0004 + 0} = \\frac{2.8506}{1.9247} \\approx 1.481$$
【/解答】

【例題】欠測メカニズム（MCAR/MAR/MNAR）を判別せよ
以下の3つの状況について、欠測メカニズムを判別し、理由を述べよ。
(a) アンケート調査で、回答者がランダムにページを飛ばして未回答となった。
(b) 健康調査で、若い人ほど体重の項目に回答しない傾向がある（年齢は全員回答済み）。
(c) 所得調査で、高所得者ほど所得を報告しない。

【解答】
(a) **MCAR**：欠測の発生は完全にランダムであり、観測値にも未観測値にも依存しない。$P(R|Y_{\\text{obs}}, Y_{\\text{mis}}) = P(R)$ が成立する。

(b) **MAR**：体重の欠測は年齢（観測されている変数）に依存するが、体重自体（欠測している値）には依存しない。$P(R|Y_{\\text{obs}}, Y_{\\text{mis}}) = P(R|Y_{\\text{obs}})$ が成立する。観測データの尤度に基づく推定（EMアルゴリズムや多重代入法）が有効。

(c) **MNAR**：所得の欠測は所得の値そのもの（未観測値）に依存している。$P(R|Y_{\\text{obs}}, Y_{\\text{mis}})$ が $Y_{\\text{mis}}$ に依存する。欠測メカニズムを明示的にモデル化する必要がある（選択モデルやパターン混合モデル等）。
【/解答】

【例題】EMアルゴリズムの収束を確認せよ
EMアルゴリズムの反復で以下の観測対数尤度が得られた。

| 反復 $t$ | $\\ell(\\theta^{(t)})$ |
|---|---|
| 0 | $-150.0$ |
| 1 | $-142.3$ |
| 2 | $-139.8$ |
| 3 | $-139.2$ |
| 4 | $-139.05$ |
| 5 | $-139.01$ |

収束判定基準 $|\\ell(\\theta^{(t+1)}) - \\ell(\\theta^{(t)})| < 0.1$ を用いて、収束の判定を行え。

【解答】
各反復での対数尤度の変化量を計算する：

| 反復 | $\\Delta\\ell = \\ell^{(t+1)} - \\ell^{(t)}$ |
|---|---|
| $0 \\to 1$ | $-142.3 - (-150.0) = 7.7$ |
| $1 \\to 2$ | $-139.8 - (-142.3) = 2.5$ |
| $2 \\to 3$ | $-139.2 - (-139.8) = 0.6$ |
| $3 \\to 4$ | $-139.05 - (-139.2) = 0.15$ |
| $4 \\to 5$ | $-139.01 - (-139.05) = 0.04$ |

対数尤度は単調に増加しており、EMアルゴリズムの理論的性質と整合する。

収束判定基準 $|\\Delta\\ell| < 0.1$ を適用すると、反復 $4 \\to 5$ で $\\Delta\\ell = 0.04 < 0.1$ となるため、反復 $t=5$ で収束と判定される。最終推定値は $\\theta^{(5)}$ となり、対応する最大観測対数尤度は $\\ell \\approx -139.01$ である。
【/解答】

【例題】2成分混合正規分布のEステップにおける負担率を計算せよ。
2成分混合正規分布 $\\pi_1 N(\\mu_1, \\sigma_1^2) + \\pi_2 N(\\mu_2, \\sigma_2^2)$ で、現在のパラメータが $\\mu_1 = 2$, $\\mu_2 = 8$, $\\sigma_1 = \\sigma_2 = 2$, $\\pi_1 = 0.4$, $\\pi_2 = 0.6$ である。データ点 $Y_i = 3$ に対する成分1の負担率 $r_{i1}$ を計算せよ。

【解答】
負担率の定義より：

$$r_{i1} = \\frac{\\pi_1 \\phi(Y_i; \\mu_1, \\sigma_1^2)}{\\pi_1 \\phi(Y_i; \\mu_1, \\sigma_1^2) + \\pi_2 \\phi(Y_i; \\mu_2, \\sigma_2^2)}$$

各成分の密度を計算する。$\\phi(x; \\mu, \\sigma^2) = \\frac{1}{\\sqrt{2\\pi}\\sigma}\\exp\\left(-\\frac{(x-\\mu)^2}{2\\sigma^2}\\right)$ より：

$$\\phi(3; 2, 4) = \\frac{1}{\\sqrt{2\\pi} \\cdot 2}\\exp\\left(-\\frac{(3-2)^2}{2 \\cdot 4}\\right) = \\frac{1}{2\\sqrt{2\\pi}}\\exp(-0.125) = 0.1995 \\times 0.8825 = 0.1760$$

$$\\phi(3; 8, 4) = \\frac{1}{2\\sqrt{2\\pi}}\\exp\\left(-\\frac{(3-8)^2}{8}\\right) = 0.1995 \\times \\exp(-3.125) = 0.1995 \\times 0.0439 = 0.00877$$

よって：

$$r_{i1} = \\frac{0.4 \\times 0.1760}{0.4 \\times 0.1760 + 0.6 \\times 0.00877} = \\frac{0.07040}{0.07040 + 0.00526} = \\frac{0.07040}{0.07566} \\approx 0.930$$

データ点 $Y_i = 3$ は成分1の平均 $\\mu_1 = 2$ に近いため、約93%の確率で成分1に帰属すると推定される。
【/解答】

【例題】EMアルゴリズムのMステップでパラメータを更新せよ。
2成分混合正規分布のEステップで以下の負担率が得られた。データは $Y = \\{1, 3, 5, 7, 9\\}$ である。

| $i$ | $Y_i$ | $r_{i1}$ | $r_{i2}$ |
|---|---|---|---|
| 1 | 1 | 0.95 | 0.05 |
| 2 | 3 | 0.85 | 0.15 |
| 3 | 5 | 0.40 | 0.60 |
| 4 | 7 | 0.10 | 0.90 |
| 5 | 9 | 0.02 | 0.98 |

Mステップで $\\pi_1^{\\text{new}}$, $\\mu_1^{\\text{new}}$, $\\sigma_1^{2\\,\\text{new}}$ を計算せよ。

【解答】
Mステップの更新式を用いる。まず $N_1 = \\sum_{i=1}^5 r_{i1} = 0.95 + 0.85 + 0.40 + 0.10 + 0.02 = 2.32$ を計算する。

$$\\pi_1^{\\text{new}} = \\frac{N_1}{n} = \\frac{2.32}{5} = 0.464$$

$$\\mu_1^{\\text{new}} = \\frac{\\sum r_{i1} Y_i}{N_1} = \\frac{0.95(1) + 0.85(3) + 0.40(5) + 0.10(7) + 0.02(9)}{2.32}$$

$$= \\frac{0.95 + 2.55 + 2.00 + 0.70 + 0.18}{2.32} = \\frac{6.38}{2.32} \\approx 2.750$$

$$\\sigma_1^{2\\,\\text{new}} = \\frac{\\sum r_{i1}(Y_i - \\mu_1^{\\text{new}})^2}{N_1}$$

$$= \\frac{0.95(1-2.75)^2 + 0.85(3-2.75)^2 + 0.40(5-2.75)^2 + 0.10(7-2.75)^2 + 0.02(9-2.75)^2}{2.32}$$

$$= \\frac{0.95(3.0625) + 0.85(0.0625) + 0.40(5.0625) + 0.10(18.0625) + 0.02(39.0625)}{2.32}$$

$$= \\frac{2.909 + 0.053 + 2.025 + 1.806 + 0.781}{2.32} = \\frac{7.574}{2.32} \\approx 3.264$$

よって更新後のパラメータは $\\pi_1 = 0.464$, $\\mu_1 = 2.750$, $\\sigma_1^2 = 3.264$ である。
【/解答】

【例題】EMアルゴリズムの収束を対数尤度の変化量で判定せよ。
2成分混合正規分布 $0.5 N(0, 1) + 0.5 N(3, 1)$ からの観測データ $Y = \\{-1, 0, 1, 2, 3, 4\\}$ について、EMの反復 $t$ と $t+1$ での対数尤度がそれぞれ $\\ell^{(t)} = -14.52$, $\\ell^{(t+1)} = -14.48$ であった。収束判定基準を相対変化量 $\\frac{|\\ell^{(t+1)} - \\ell^{(t)}|}{|\\ell^{(t)}|} < 10^{-3}$ として収束を判定せよ。

【解答】
対数尤度の変化量を計算する：

$$\\Delta\\ell = \\ell^{(t+1)} - \\ell^{(t)} = -14.48 - (-14.52) = 0.04$$

まず $\\Delta\\ell = 0.04 > 0$ なので、対数尤度は増加しており、EMの単調増加性と整合している。

相対変化量を計算する：

$$\\frac{|\\Delta\\ell|}{|\\ell^{(t)}|} = \\frac{0.04}{14.52} \\approx 0.00275$$

$0.00275 > 10^{-3} = 0.001$ であるから、収束判定基準を満たさない。EMの反復を継続する必要がある。

なお、別の基準としてパラメータの変化量 $\\|\\theta^{(t+1)} - \\theta^{(t)}\\| < \\varepsilon$ を用いることもある。複数の基準を組み合わせて判定することが実務的には推奨される。また、対数尤度の絶対変化量 $|\\Delta\\ell| < 0.01$ を基準にすると $0.04 > 0.01$ でやはり未収束と判定される。
【/解答】

【例題】MAR条件下での尤度ベース推定を説明し具体例を計算せよ。
2変量正規分布 $(Y_1, Y_2) \\sim N_2(\\boldsymbol{\\mu}, \\boldsymbol{\\Sigma})$ から5個体を観測したが、$Y_2$ にMARの欠測がある。完全データが3個体、$Y_1$ のみ観測が2個体であった。

| 個体 | $Y_1$ | $Y_2$ |
|---|---|---|
| 1 | 2 | 4 |
| 2 | 4 | 6 |
| 3 | 3 | 5 |
| 4 | 5 | ? |
| 5 | 6 | ? |

MAR仮定の下で、観測データの尤度関数を構成せよ。

【解答】
MARでは欠測メカニズムが無視可能であり、観測データの尤度のみでパラメータ推定が可能である。

観測データの尤度は、完全観測データの同時密度と不完全データの周辺密度の積で構成される：

$$L(\\boldsymbol{\\mu}, \\boldsymbol{\\Sigma}) = \\prod_{i=1}^{3} f(y_{i1}, y_{i2}; \\boldsymbol{\\mu}, \\boldsymbol{\\Sigma}) \\times \\prod_{i=4}^{5} f(y_{i1}; \\mu_1, \\sigma_{11})$$

第1項は完全観測された3個体の2変量正規密度：

$$f(y_1, y_2) = \\frac{1}{2\\pi|\\boldsymbol{\\Sigma}|^{1/2}}\\exp\\left(-\\frac{1}{2}(\\mathbf{y}-\\boldsymbol{\\mu})^\\top\\boldsymbol{\\Sigma}^{-1}(\\mathbf{y}-\\boldsymbol{\\mu})\\right)$$

第2項は $Y_2$ を周辺化した $Y_1$ の周辺密度：

$$f(y_1) = \\frac{1}{\\sqrt{2\\pi\\sigma_{11}}}\\exp\\left(-\\frac{(y_1 - \\mu_1)^2}{2\\sigma_{11}}\\right)$$

ここで $\\sigma_{11}$ は $\\boldsymbol{\\Sigma}$ の $(1,1)$ 成分である。

この尤度関数は全5個体の情報を有効に利用しており、リストワイズ削除（完全データ3個体のみ使用）よりも効率的な推定が可能である。EMアルゴリズムでは、Eステップで $Y_2$ の欠測値の条件付き期待値 $E[Y_2 | Y_1, \\theta^{(t)}]$ を計算し、Mステップで補完されたデータから $\\boldsymbol{\\mu}$ と $\\boldsymbol{\\Sigma}$ を更新する。
【/解答】

【例題】多重代入法の概念を説明し、信頼区間を統合せよ。
ある調査で欠測データに対して $m = 5$ 回の多重代入を行い、パラメータ $\\theta$（母平均）について以下の推定値と標準誤差を得た。

| 代入 $l$ | $\\hat{\\theta}_l$ | $\\hat{\\text{SE}}_l$ |
|---|---|---|
| 1 | 10.2 | 1.5 |
| 2 | 9.8 | 1.4 |
| 3 | 10.5 | 1.6 |
| 4 | 10.0 | 1.5 |
| 5 | 9.5 | 1.3 |

Rubinの統合公式を用いて統合推定値と95%信頼区間を求めよ。

【解答】
Rubinの統合公式に従う。

**統合推定値**：

$$\\bar{\\theta} = \\frac{1}{m}\\sum_{l=1}^m \\hat{\\theta}_l = \\frac{10.2 + 9.8 + 10.5 + 10.0 + 9.5}{5} = \\frac{50.0}{5} = 10.0$$

**代入内分散（Within-imputation variance）**：

$$\\bar{W} = \\frac{1}{m}\\sum_{l=1}^m \\hat{\\text{SE}}_l^2 = \\frac{1.5^2 + 1.4^2 + 1.6^2 + 1.5^2 + 1.3^2}{5} = \\frac{2.25 + 1.96 + 2.56 + 2.25 + 1.69}{5} = \\frac{10.71}{5} = 2.142$$

**代入間分散（Between-imputation variance）**：

$$B = \\frac{1}{m-1}\\sum_{l=1}^m (\\hat{\\theta}_l - \\bar{\\theta})^2 = \\frac{(0.2)^2 + (-0.2)^2 + (0.5)^2 + (0)^2 + (-0.5)^2}{4} = \\frac{0.04 + 0.04 + 0.25 + 0 + 0.25}{4} = \\frac{0.58}{4} = 0.145$$

**統合分散（Total variance）**：

$$T = \\bar{W} + \\left(1 + \\frac{1}{m}\\right)B = 2.142 + 1.2 \\times 0.145 = 2.142 + 0.174 = 2.316$$

**自由度**（Barnard-Rubinの近似）：

$$\\nu = (m - 1)\\left(1 + \\frac{\\bar{W}}{(1 + 1/m)B}\\right)^2 = 4 \\times \\left(1 + \\frac{2.142}{0.174}\\right)^2 = 4 \\times (1 + 12.31)^2 = 4 \\times 177.4 \\approx 710$$

十分大きいので正規近似が利用可能。95%信頼区間は：

$$\\bar{\\theta} \\pm 1.96\\sqrt{T} = 10.0 \\pm 1.96 \\times \\sqrt{2.316} = 10.0 \\pm 1.96 \\times 1.522 = 10.0 \\pm 2.983$$

$$\\text{95\\%信頼区間} = [7.02,\\; 12.98]$$

欠測の影響を反映するため、代入間分散 $B$ により信頼区間が広がっている点が多重代入法の特徴である。
【/解答】

【例題】EMアルゴリズムの収束速度と情報行列の関係を説明せよ。
2成分混合正規分布 $\\pi N(\\mu_1, 1) + (1-\\pi)N(\\mu_2, 1)$ のEMアルゴリズムにおいて、完全データの対数尤度に基づく情報行列（完全情報）$I_c(\\theta)$ と観測データの情報行列 $I_{\\text{obs}}(\\theta)$ が以下で与えられたとする。

$$I_c(\\theta) = \\begin{pmatrix} 50 & 0 \\\\ 0 & 50 \\end{pmatrix}, \\quad I_{\\text{obs}}(\\theta) = \\begin{pmatrix} 35 & -5 \\\\ -5 & 30 \\end{pmatrix}$$

欠測情報行列 $I_{\\text{mis}}(\\theta)$ を求め、EMアルゴリズムの収束速度を評価せよ。

【解答】
欠測情報の原理（Missing Information Principle）より：

$$I_{\\text{obs}}(\\theta) = I_c(\\theta) - I_{\\text{mis}}(\\theta)$$

したがって：

$$I_{\\text{mis}}(\\theta) = I_c(\\theta) - I_{\\text{obs}}(\\theta) = \\begin{pmatrix} 50 & 0 \\\\ 0 & 50 \\end{pmatrix} - \\begin{pmatrix} 35 & -5 \\\\ -5 & 30 \\end{pmatrix} = \\begin{pmatrix} 15 & 5 \\\\ 5 & 20 \\end{pmatrix}$$

EMアルゴリズムの収束速度はレート行列 $\\mathbf{R} = I_{\\text{mis}}(\\theta) I_c(\\theta)^{-1}$ の最大固有値で決まる。

$$I_c(\\theta)^{-1} = \\frac{1}{50}\\mathbf{I}_2 = \\begin{pmatrix} 0.02 & 0 \\\\ 0 & 0.02 \\end{pmatrix}$$

$$\\mathbf{R} = \\begin{pmatrix} 15 & 5 \\\\ 5 & 20 \\end{pmatrix}\\begin{pmatrix} 0.02 & 0 \\\\ 0 & 0.02 \\end{pmatrix} = \\begin{pmatrix} 0.30 & 0.10 \\\\ 0.10 & 0.40 \\end{pmatrix}$$

$\\mathbf{R}$ の固有値を求める。特性方程式 $(0.30 - \\lambda)(0.40 - \\lambda) - 0.01 = 0$ より：

$$\\lambda^2 - 0.70\\lambda + 0.11 = 0$$

$$\\lambda = \\frac{0.70 \\pm \\sqrt{0.49 - 0.44}}{2} = \\frac{0.70 \\pm 0.2236}{2}$$

$$\\lambda_1 \\approx 0.462, \\quad \\lambda_2 \\approx 0.238$$

最大固有値 $\\lambda_{\\max} = 0.462$ がEMの収束レートである。これは線形収束であり、1反復ごとにパラメータの誤差が約0.46倍に減少する。$\\lambda_{\\max}$ が1に近いほど収束は遅く、0に近いほど速い。本例では中程度の速度であり、欠測情報が全情報の約30-40%を占めていることを反映している。
【/解答】

【例題】完全データ対数尤度と観測データ対数尤度の関係を導出せよ。
確率変数 $Y$ が2成分混合分布 $f(y;\\theta) = 0.5 f_1(y;\\mu_1) + 0.5 f_2(y;\\mu_2)$ に従い、潜在変数 $Z \\in \\{1, 2\\}$ が成分帰属を表す。観測値 $y = 3$ に対して、現在のパラメータ推定値 $\\mu_1^{(t)} = 1$, $\\mu_2^{(t)} = 5$（分散は既知で1）のもとで、完全データ対数尤度 $\\log L_c(\\theta; y, z)$、観測データ対数尤度 $\\log L_{\\text{obs}}(\\theta; y)$、および $Q$ 関数の関係を具体的に示せ。

【解答】
完全データの対数尤度は潜在変数 $Z$ を含む：

$$\\log L_c(\\theta; y, z) = \\log\\left[\\pi_z \\cdot \\phi(y; \\mu_z, 1)\\right] = \\log \\pi_z + \\log \\phi(y; \\mu_z, 1)$$

$z = 1$ のとき：$\\log L_c = \\log 0.5 + \\log \\phi(3; \\mu_1, 1) = -0.693 - \\frac{1}{2}\\log(2\\pi) - \\frac{(3-\\mu_1)^2}{2}$

$z = 2$ のとき：$\\log L_c = \\log 0.5 + \\log \\phi(3; \\mu_2, 1) = -0.693 - \\frac{1}{2}\\log(2\\pi) - \\frac{(3-\\mu_2)^2}{2}$

観測データの対数尤度は $Z$ を周辺化して：

$$\\log L_{\\text{obs}}(\\theta; y) = \\log\\left[0.5\\phi(3; \\mu_1, 1) + 0.5\\phi(3; \\mu_2, 1)\\right]$$

$Q$ 関数はEステップで計算される完全データ対数尤度の条件付き期待値である。現在の推定値での負担率を計算する：

$$r_1 = \\frac{0.5 \\cdot \\phi(3; 1, 1)}{0.5 \\cdot \\phi(3; 1, 1) + 0.5 \\cdot \\phi(3; 5, 1)} = \\frac{\\phi(3; 1, 1)}{\\phi(3; 1, 1) + \\phi(3; 5, 1)}$$

$$\\phi(3; 1, 1) = \\frac{1}{\\sqrt{2\\pi}}e^{-2} \\approx 0.0540, \\quad \\phi(3; 5, 1) = \\frac{1}{\\sqrt{2\\pi}}e^{-2} \\approx 0.0540$$

対称性から $r_1 = r_2 = 0.5$ となる。

$Q$ 関数は：

$$Q(\\theta|\\theta^{(t)}) = r_1 \\log L_c(\\theta; y, z=1) + r_2 \\log L_c(\\theta; y, z=2)$$

$$= 0.5\\left[-0.693 - \\frac{1}{2}\\log(2\\pi) - \\frac{(3-\\mu_1)^2}{2}\\right] + 0.5\\left[-0.693 - \\frac{1}{2}\\log(2\\pi) - \\frac{(3-\\mu_2)^2}{2}\\right]$$

重要な関係式として、以下が成立する：

$$\\log L_{\\text{obs}}(\\theta; y) = Q(\\theta|\\theta^{(t)}) - H(\\theta|\\theta^{(t)})$$

ここで $H(\\theta|\\theta^{(t)}) = E[\\log P(Z|Y, \\theta) | Y, \\theta^{(t)}]$ はエントロピー項である。EMアルゴリズムの各反復で $Q$ 関数を最大化することが、$\\log L_{\\text{obs}}$ の単調増加を保証する。
【/解答】`
    }
  ]
};
